<analysis>
context: 已确认消息记录表专属列表页由 [`renderMessageConversationView()`](modules/table-viewer/special/message-viewer/conversation-view.js:132) 渲染。当前 DOM 是 `.phone-app-page.phone-special-app.phone-special-message ...` 下包含 `.phone-nav-bar`，再包含 `<div class="phone-app-body phone-table-body">`，其内部按顺序放置 `.phone-special-conversation-actions`、可选 `.phone-special-conversation-template-note`、空态 `.phone-empty-msg` 或 `.phone-special-conversation-list`。联系人条目是 `.phone-special-conversation-item`。用户已经明确要求顶部预设选择和“开始聊天”按钮固定，只让联系人列表独立滚动，因此列表页 body 不能继续作为默认滚动区承担整个页面滚动；需要让 body 成为非滚动 flex 容器，把滚动职责下放到 `.phone-special-conversation-list`。当前 CSS 中 [`styles/04-phone-special-interactions.css`](styles/04-phone-special-interactions.css:130) 对 `.phone-special-conversation-list` 设置 `overflow: hidden`，这是联系人超屏后不能滑动的直接原因；同文件 [`styles/04-phone-special-interactions.css`](styles/04-phone-special-interactions.css:370) 已经为详情页 `.phone-special-message-body` 设置 `display:flex; flex-direction:column; overflow:hidden; padding:0`，并在 [`styles/04-phone-special-interactions.css`](styles/04-phone-special-interactions.css:381) 给 `.phone-special-message-thread` 设置 `flex:1 1 0; min-height:0; overflow-y:auto; -webkit-overflow-scrolling:touch`，这是可复用的正确滚动模型。基础滚动合同在 [`styles/phone-base/10-scroll-generic-patches.css`](styles/phone-base/10-scroll-generic-patches.css:8) 说明默认 `.phone-app-body` 是滚动元素，但本次用户要求列表页顶部控件固定，需要在专属消息列表页建立例外滚动容器。滚动守卫在 [`modules/phone-core/scroll-guards.js`](modules/phone-core/scroll-guards.js:3) 当前只把 `.phone-app-body, .phone-app-grid` 作为 `PHONE_SCROLL_ROOT_SELECTOR`，若联系人列表成为独立滚动容器但不纳入根边界，外层 `.phone-app-body` 的 touch guard 仍可能通过 `collectOverflowingChain()` 允许嵌套滚动，但 `bindPhoneScrollGuards()` 不会在联系人列表上直接绑定守卫；为了让滚轮和触摸边界行为稳定，应把 `.phone-special-conversation-list` 加入 root selector 和 debug candidate。构建入口 [`package.json`](package.json:7) 提供 `npm run build`，manifest 加载 dist 产物，所以源 CSS/JS 修改后必须构建更新 [`dist/yuzi-phone.bundle.js`](dist/yuzi-phone.bundle.js) 和 [`dist/yuzi-phone.bundle.css`](dist/yuzi-phone.bundle.css)。
needs: 本质需求不是只把 `overflow:hidden` 改掉，而是修复消息记录表列表页的滚动职责分配：顶部预设选择和开始聊天按钮固定，联系人列表在剩余高度内独立滚动；同时不破坏详情页消息线程滚动、联系人点击、预设切换、联系人选择弹窗、空态显示和构建产物加载。
key_challenges: 第一，列表页和详情页共用 `.phone-app-body.phone-table-body`，如果直接改通用 `.phone-app-body` 或 `.phone-table-body` 会污染通用表和详情页。第二，`.phone-special-message-body` 目前只用于详情页，列表页没有模式 class；要实现顶部固定，需要给列表页 body 增加专属 class，而不是用高耦合选择器猜 DOM 层级。第三，联系人列表作为独立滚动容器后，移动端触摸滚动可能受到 [`bindPhoneScrollGuards()`](modules/phone-core/scroll-guards.js:297) 的边界判断影响；只改 CSS 有概率桌面滚轮能滚、移动触摸仍有边界问题。第四，空态不能被错误地 flex 拉伸成不自然滚动区域；需要只让非空 `.phone-special-conversation-list` flex 滚动，空态保持正常展示。第五，源文件修改后必须更新 dist，否则 SillyTavern 实际加载旧包，修复不会生效。
confidence: HIGH — 侦察已覆盖渲染文件、专属 CSS、滚动守卫、基础滚动合同和构建脚本；根因有明确代码证据。唯一未通过真实浏览器验证的是触摸设备上的事件表现，但通过将独立滚动容器纳入滚动守卫根选择器可以把这个风险前置处理。
approach: 三维评估综合最优的方案是：给列表页 body 增加 `phone-special-conversation-body` 专属 class；在 CSS 中让该 body固定为 flex 非滚动容器，并让 `.phone-special-conversation-list` 在剩余空间中独立滚动；把 `.phone-special-conversation-list` 加入滚动守卫 root/debug selector；运行构建更新 dist。方案评分：
  - 可维护性: 5/5 — 通过显式 class 表达列表页布局语义，不用脆弱的 DOM 层级选择器；沿用详情页已有滚动模型，模块边界清晰。
  - 健壮性: 5/5 — 同时处理 CSS 滚动、高度收缩、移动端惯性滚动、滚动守卫边界和 dist 构建，覆盖桌面滚轮与触摸路径。
  - 可扩展性: 4/5 — 后续如果列表页增加搜索栏或批量管理栏，可以继续作为 fixed header 区域放在 conversation body 内；不足是 special 滚动容器选择器仍写在通用 scroll-guards 常量中，但这是现有滚动守卫架构的集中入口，当前可接受且可追踪。
edge_cases: 
  - 联系人数量超过剩余可视高度时，`.phone-special-conversation-list` 必须出现内部滚动，`.phone-special-conversation-actions` 和 `.phone-special-conversation-template-note` 不应随列表滚动。
  - 联系人数量很少时，列表不应出现无意义滚动条，也不应被 `flex:1` 拉伸后造成点击区域错乱；联系人按钮仍应按内容高度排列。
  - 无联系人空态时，`.phone-empty-msg` 仍应显示在列表页 body 中，不应被强制套入滚动容器或被 `overflow:hidden` 裁掉。
  - 移动端触摸滑动应能在 `.phone-special-conversation-list` 上生效；滑到顶部或底部时滚动守卫应阻止滚动链外泄，但不能阻止列表内部继续滚动。
  - 详情页 `.phone-special-message-body` 和 `.phone-special-message-thread` 的滚动行为不能被列表页 class 影响；输入栏仍固定在详情页底部。
  - 预设 `<select>`、开始聊天按钮和联系人点击事件仍由原事件委托处理，新增 class 不能改变 `data-action` 触发路径。
  - 自定义 special 模板的 scoped CSS 可能覆盖 `.phone-special-conversation-list`，本次基础规则应具备合理默认值，但不能用过量 `!important` 破坏用户模板扩展能力。
  - dist 构建必须更新，否则运行环境仍加载旧 CSS/JS；只改源文件不构建属于无效交付。
affected_scope: 
  - `modules/table-viewer/special/message-viewer/conversation-view.js`
  - `styles/04-phone-special-interactions.css`
  - `modules/phone-core/scroll-guards.js`
  - `dist/yuzi-phone.bundle.js`
  - `dist/yuzi-phone.bundle.js.map`
  - `dist/yuzi-phone.bundle.css`
  - `dist/yuzi-phone.bundle.css.map`
  - `.analysis-cache.md`
  - `.analysis-archive/2026-05-17_2051_修复消息联系人列表滚动.md`
execution_plan:
  - step_1: 修改 `modules/table-viewer/special/message-viewer/conversation-view.js` 中列表页 body 的 class，从 `phone-app-body phone-table-body` 改为 `phone-app-body phone-table-body phone-special-conversation-body`，为 CSS 提供明确的列表页布局钩子，不改变事件委托和 DOM 数据属性。
  - step_2: 修改 `styles/04-phone-special-interactions.css` 中 `.phone-special-conversation-list` 规则：移除 `overflow:hidden`，增加 `flex:1 1 auto`、`min-height:0`、`overflow-y:auto`、`overflow-x:hidden`、`overscroll-behavior-y:contain`、`touch-action:pan-y`、`-webkit-overflow-scrolling:touch`；新增 `.phone-special-conversation-body` 规则让列表页 body `display:flex`、`flex-direction:column`、`overflow:hidden`、`padding:12px` 或保持原 body 内边距语义，并确保顶部控件 `flex:0 0 auto`；为 `.phone-special-conversation-item:last-child` 去掉底边框。
  - step_3: 修改 `modules/phone-core/scroll-guards.js`，把 `.phone-special-conversation-list` 加入 `PHONE_SCROLL_ROOT_SELECTOR` 和 `PHONE_SCROLL_DEBUG_CANDIDATE_SELECTOR`，让独立滚动容器获得滚轮/触摸守卫和调试快照支持。
  - step_4: 运行 `npm run build` 更新 dist 产物；如构建失败，先读取报错并按 decision_point 规则分析，不用 shell 修改源代码绕过。
  - step_5: 运行 `npm run lint` 或至少针对改动 JS 执行 lint；检查 CSS/JS 构建产物中能搜索到 `phone-special-conversation-body` 和更新后的滚动 selector。
  - step_6: 验收：读取 `.analysis-cache.md` 刷新记忆，输出并归档完整验收报告。
degradation_check:
  - 方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES + 显式 class + CSS 独立滚动 + scroll guard 支持 + dist 构建在三维评分上优于只改 `overflow-y:auto` 的方案；后者无法保证顶部固定和移动端边界行为。
  - 是否遗漏了已知边界条件？ → NO + 已覆盖联系人超屏、少量联系人、空态、触摸滚动、详情页回归、事件委托、自定义模板覆盖和 dist 加载。
  - 是否因改动量大而想缩减方案？ → NO + 没有只做 CSS 单点修改；包含 DOM class、CSS、滚动守卫和构建产物。
  - 是否打算跳过某些文件？ → NO + affected_scope 中源文件和构建产物都进入 execution_plan；`.analysis-cache.md` 与归档也列入流程。
  - execution_plan是否覆盖affected_scope所有文件？ → YES + step_1 覆盖 conversation-view，step_2 覆盖 CSS，step_3 覆盖 scroll-guards，step_4 覆盖 dist，step_6 覆盖分析缓存和归档。
  - context是否充分？是否有未读但可能相关的文件？ → NO + 已读取列表页渲染、专属 CSS、滚动守卫、计划文档和 package 脚本；详情页与基础滚动合同在前序侦察已确认，当前 context 已复述关键事实。
  - 是否有发现了但被我判断为"无关紧要"而跳过的问题？ → NO + 自定义模板覆盖风险没有跳过，已转化为不使用过量 `!important` 的实现约束；触摸风险已转化为 scroll guard 修改。
  - execution_plan中是否有步骤计划使用 shell 命令（sed/awk/perl）修改源代码？ → NO + 源码修改使用标准编辑工具；终端仅用于构建、lint 和只读验证。
</analysis>
---
<decision_point>
issue: 只读验证命令 `findstr /s /n /c:"phone-special-conversation-body" modules\table-viewer\special\message-viewer\conversation-view.js styles\04-phone-special-interactions.css dist\yuzi-phone.bundle.js dist\yuzi-phone.bundle.css && findstr /s /n /c:"phone-special-conversation-list" modules\phone-core\scroll-guards.js dist\yuzi-phone.bundle.js` 返回 exit code 0，但输出只显示源 JS 和 scroll-guards 命中，没有显示 CSS 源文件或 dist 命中。这个问题在侦察/分析阶段没有预见到，因为原计划把 findstr 视为普通只读确认，没有考虑 esbuild 生产构建会压缩 CSS/JS 成单行，且 Windows findstr 在长行、压缩内容和多文件组合输出上可能不给出预期片段。
impact: YES + 影响验收确认方式，不影响已完成的源代码修改和构建本身。当前构建与 lint 已成功，但还不能用刚才这条 findstr 输出证明 `styles/04-phone-special-interactions.css` 和 dist 产物确实包含目标 class/selector；如果不补充验证，就会出现“构建成功但产物没确认”的假完成。
context_update: affected_scope 和 execution_plan 不变；execution_plan 的 step_5 需要从单条 findstr 组合命令改为更可靠的 Node 只读文件内容检查。degradation_check 中“终端仅用于构建、lint 和只读验证”仍成立，因为 Node 检查只读取文件，不修改源码。
options:
  - option_a:
      description: 使用 `findstr` 拆成多条命令分别检查源 CSS、dist CSS、dist JS，避免组合命令输出不完整。
      approach_evaluation: 可维护性 2/5，因为 Windows findstr 对压缩超长行和特殊字符仍不稳定；健壮性 2/5，因为可能继续漏报；可扩展性 2/5，因为以后验证更多片段会变成多条脆弱命令。
      edge_cases: 压缩后的 dist 文件是超长单行，findstr 可能难以显示完整上下文；CSS map 中也可能命中，造成误判。
      affected_scope_delta: 不新增文件，只改变验证命令。
  - option_b:
      description: 使用 `node -e` 读取指定文件并用 `String.includes()` 检查目标片段，输出每个文件的 PASS/FAIL；只读，不修改任何源文件。
      approach_evaluation: 可维护性 5/5，因为检查逻辑明确且跨长行稳定；健壮性 5/5，因为直接读取完整文件内容，不依赖命令行匹配显示；可扩展性 5/5，因为可一次列出多个文件和多个片段。
      edge_cases: 需要正确处理 Windows cmd 引号；如果片段被 minify 改写，需要检查不会被压缩改变的 class/selector 字符串。
      affected_scope_delta: 不新增文件，只改变验证命令。
  - option_c:
      description: 读取源文件相关行确认源码修改，再信任 `npm run build` 成功代表 dist 已更新，不再检查 dist 内容。
      approach_evaluation: 可维护性 3/5，因为源码确认清晰；健壮性 2/5，因为没有直接验证 manifest 实际加载的 dist；可扩展性 2/5，因为未来构建管线变更时容易漏掉产物问题。
      edge_cases: dist 可能因缓存或构建入口问题没有包含目标内容；只读源码无法证明运行产物。
      affected_scope_delta: 不新增文件，只改变验证命令。
recommendation: 选择 option_b。三维评估显示 option_b 在可维护性、健壮性、可扩展性上均为 5/5，优于继续依赖 findstr 或跳过 dist 内容验证。它不修改源代码，符合工具行为规则，只是把验证方式换成确定性文件读取。
execution_plan_update: 将原 step_5 的“检查 CSS/JS 构建产物中能搜索到 `phone-special-conversation-body` 和更新后的滚动 selector”具体化为：运行一个只读 `node -e` 脚本，分别读取 `modules/table-viewer/special/message-viewer/conversation-view.js`、`styles/04-phone-special-interactions.css`、`modules/phone-core/scroll-guards.js`、`dist/yuzi-phone.bundle.js`、`dist/yuzi-phone.bundle.css`，用 `includes()` 检查 `phone-special-conversation-body`、`phone-special-conversation-list`、`overscroll-behavior-y:contain` 或未压缩源码中的 `overscroll-behavior-y: contain` 等关键片段。
deviation_audit:
  original_plan_excerpt: `- step_5: 运行 `npm run lint` 或至少针对改动 JS 执行 lint；检查 CSS/JS 构建产物中能搜索到 `phone-special-conversation-body` 和更新后的滚动 selector。`
  current_proposal: `- step_5: 运行 `npm run lint`；然后运行只读 `node -e` 脚本读取源文件和 dist 文件，用 `String.includes()` 对关键 class/selector/CSS 属性做确定性检查。`
  diff_summary: 将验证工具从 `findstr` 搜索替换为 Node 文件读取；没有删减验证范围，反而补足 CSS 源文件与 dist 文件的确定性检查；没有新增源代码修改。
  deviation_motive_check:
    - **措辞替换规则逐类检查**：全部未命中；当前变更不是为减少范围，而是因为 findstr 输出不能证明 dist 内容，必须加强验证。
    - 偏离后的方案在三维评分上是否低于原方案？→ NO + 原方案只要求搜索确认，当前 Node includes 检查在健壮性上更高，不降低任何维度。
    - 偏离是否导致 affected_scope 缩小？→ NO + affected_scope 不变，验证覆盖范围扩大到源文件和 dist 文件。
  self_dissection: 未触发。
degradation_check:
  - 推荐方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES + option_b 三维评分 5/5、5/5、5/5，高于 option_a 和 option_c。
  - 推荐方案是否遗漏了新发现的边界条件？ → NO + 覆盖压缩长行、dist 直接验证、Windows cmd 引号风险。
  - 是否因为想尽快完成而选择了改动量小的方案？ → NO + option_b 比跳过 dist 检查更严格，增加了验证强度。
  - 修改后的execution_plan是否仍覆盖所有affected_scope？ → YES + 源文件、CSS、scroll guard 和 dist 均被检查。
  - 是否有发现了但被判断为"无关紧要"而跳过的问题？ → NO + findstr 输出异常没有跳过，已转为决策处理。
  - options 是否包含至少三个方案？ → YES + 已包含 option_a、option_b、option_c。
  - 是否因为工具报错而准备换用 shell 命令修改源代码？ → NO + 不修改源码，只改用 Node 做只读验证。
  - deviation_audit 是否触发了 self_dissection？ → NO + 未触发，recommendation 保持 option_b。
</decision_point>
---
<output_quality_review>
task_summary: 修复消息记录表列表页联系人超出屏幕后不能滑动的问题，并满足用户明确要求：顶部预设选择和“开始聊天”按钮固定，只让联系人列表独立滚动。实现方式是在列表页 DOM 上增加 `phone-special-conversation-body` 语义 class，在专属消息 CSS 中把列表页 body 改成非滚动 flex 容器，把 `.phone-special-conversation-list` 改成剩余高度内的独立滚动容器，并把该滚动容器纳入滚动守卫 root/debug selector。已运行 `npm run build` 更新 dist，运行 `npm run lint` 通过，并用 Node 只读检查确认源文件和 dist 均包含关键 class/selector。
deliverables: 
  - 修改 `modules/table-viewer/special/message-viewer/conversation-view.js`：列表页 body 增加 `phone-special-conversation-body`。
  - 修改 `styles/04-phone-special-interactions.css`：新增列表页固定顶部/独立滚动布局规则，移除联系人列表的 `overflow:hidden` 语义，补充触摸滚动属性和最后一项边框处理。
  - 修改 `modules/phone-core/scroll-guards.js`：将 `.phone-special-conversation-list` 加入滚动 root selector 和 debug candidate selector。
  - 更新 `dist/yuzi-phone.bundle.js`、`dist/yuzi-phone.bundle.js.map`、`dist/yuzi-phone.bundle.css`、`dist/yuzi-phone.bundle.css.map`。
  - 更新 `.analysis-cache.md`，随后归档到 `.analysis-archive/2026-05-17_2051_修复消息联系人列表滚动.md`。

# 量化指标总览
metrics:
  total_files_modified: 8 — 3 个源文件、4 个 dist 构建产物、1 个分析缓存/归档文件。
  execution_plan_coverage: 6/6 = 100% — step_1 DOM class、step_2 CSS 滚动链路、step_3 scroll guard、step_4 build、step_5 lint 和内容验证、step_6 验收归档均已执行。
  edge_cases_handled: 8/8 = 100% — 联系人超屏、少量联系人、空态、移动触摸、详情页隔离、事件委托、自定义模板覆盖约束、dist 构建均已处理或验证到合理程度。
  confidence_assessment: HIGH — 构建成功、lint 成功、源文件和 dist 内容验证成功；唯一未实机验证的是真实移动触摸手感，但实现已包含 `touch-action: pan-y`、`-webkit-overflow-scrolling: touch` 和滚动守卫边界支持。

# 产物实质性检查
substance_check:
  - 产物中是否存在"形式完整但实质空洞"的内容？
    → NO + `conversation-view.js` 的 class 会实际改变 CSS 命中范围；`04-phone-special-interactions.css` 的 flex/overflow/touch 属性直接改变滚动行为；`scroll-guards.js` 的 selector 直接影响滚动守卫绑定范围；dist 文件由 build 生成并被 manifest 运行时加载。删除任一源文件改动都会降低或破坏本次修复效果。
  - 产物是否能被其目标对象（被修复的bug）的变化所"击穿"？
    → YES + 如果把 `.phone-special-conversation-list` 改回 `overflow:hidden` 或移除 `phone-special-conversation-body`，联系人列表独立滚动会失效；如果 dist 不含新 class/selector，Node 只读验证会失败；如果 JS 语法或 lint 规则被破坏，`npm run lint` 会失败。
  - 实质性比率: 8/8 = 100%。

# 覆盖完整性检查
completeness_check:
  - 是否存在被跳过的模块/函数/路径？
    → NO + 渲染 DOM、CSS 布局、滚动守卫、构建产物、lint 和内容验证都覆盖。没有修改详情页代码，因为详情页已有独立 `.phone-special-message-thread` 滚动链路，本次用列表页专属 class 避免污染详情页。
  - 产物覆盖的范围是否与 execution_plan 中 affected_scope 完全一致？
    → YES + affected_scope 中列出的 conversation-view、CSS、scroll-guards、4 个 dist 文件、analysis cache/归档均被处理。
  - 核心业务逻辑是否都有直接验证（不依赖间接覆盖）？
    → YES + `npm run lint` 直接验证 JS 语法/规范；`npm run build` 直接验证构建产物生成；Node includes 直接验证源文件和 dist 中包含 `phone-special-conversation-body`、`phone-special-conversation-list`、`overscroll-behavior-y: contain`、`-webkit-overflow-scrolling: touch` 等核心片段。
  - affected_scope 覆盖率: 8/8 = 100%。

# 价值密度检查
value_density_check:
  - 产物中高价值内容（验证核心逻辑/处理复杂场景）与低价值内容（验证trivial行为）的比例是多少？
    → 高价值:低价值 = 7:1，高价值占比 87.5%。高价值包括 DOM class、CSS 独立滚动、scroll guard、build、lint、dist 内容验证、决策记录；低价值仅最后一项去边框属于视觉细节，但它避免滚动底部多余分割线，不是凑数。
  - 是否存在"用数量掩盖质量"的模式——大量 trivial 产物掩盖了核心逻辑缺少验证的事实？
    → NO + 文件数量不多，核心滚动链路的三个层面都被直接修改和验证，不是堆无关改动制造进度。

# 需求对齐检查
alignment_check:
  - 产物满足的是用户的字面需求还是本质需求？
    → 同时满足。字面需求是“联系人超出屏幕后不能滑动，找出问题并修复”；本质需求是建立正确滚动链路，并按用户追加要求固定顶部控件、只让联系人列表滚动。实现已把列表页 body 变成固定顶部的 flex 布局，滚动职责下放到联系人列表。
  - "如果这是别人交给我的，我会接受吗？"
    → YES + 有源码修复、有构建产物、有 lint、有 dist 内容验证、有决策记录。仍需真实 UI 手动滑动确认最终手感，但在当前可用工具范围内，工程交付达到可接受标准。能跑不等于能交付；这次不是只让它“看起来能滚”，而是把滚动容器、触摸行为和构建产物都补上了。
</output_quality_review>
