<analysis>
context: 本轮收尾审查已读取小日历 scene、CSS、小剧场编辑桥、路由、合同脚本和文档。当前 [`calendar.js`](modules/phone-theater/scenes/calendar.js:277) 已使用 `YEAR_PICKER_RANGE = 50`、`buildYearOptions()`、`renderYearPicker()`、`setYearPickerOpen()`、`renderYearOptions()` 和点击分发处理自定义年份 picker；文件内未出现 `<select` 或 `HTMLSelectElement`。[`calendar.css`](styles/phone-theater/calendar.css:89) 已使用 `.phone-theater-calendar-year-picker`、`.phone-theater-calendar-year-toggle`、`.phone-theater-calendar-year-panel`、`.phone-theater-calendar-year-option` 等内部控件样式，旧的 `.phone-theater-calendar-month-select select` 与 `select option` 未在 calendar CSS 中残留。通用编辑桥在 [`interactions.js`](modules/phone-theater/interactions.js:187) 中统一调用 `navigateTo(\`table-generic:${sheetKey}\`)`，calendar scene 只声明 [`editableTables`](modules/phone-theater/scenes/calendar.js:451)，没有在 scene 内手搓编辑跳转。路由层 [`route-renderer.js`](modules/phone-core/route-renderer.js:64) 已将 `table-generic:` 独立于普通 `app:` 分支处理，并以 `forceGenericList: true` 调用 [`renderTableViewer()`](modules/table-viewer/render.js:23)；普通 `app:` 分支会对小剧场子表兜底到 [`renderTheaterScene()`](modules/phone-core/route-renderer.js:46)，不会污染编辑桥。[`renderTableViewer()`](modules/table-viewer/render.js:80) 在 `forceGenericList` 时跳过 special 检测并传递 `forceListMode`。文档方面，[`docs/architecture-guide.md`](docs/architecture-guide.md:130) 已记录小剧场编辑桥与 route history 返回语义；[`docs/reference/theater-scene-extension-spec.md`](docs/reference/theater-scene-extension-spec.md:79) 已记录 `editableTables` 编辑桥契约。合同脚本 [`check-theater-contract.cjs`](scripts/check-theater-contract.cjs:347) 到 [`check-theater-contract.cjs`](scripts/check-theater-contract.cjs:350) 已检查禁止原生年份 select、自定义 picker 结构、滚动样式和 calendar CSS 不依赖原生 select。搜索结果显示 `<select` 和 `HTMLSelectElement` 的其他命中来自通用表、设置页、fusion、special message viewer 等既有合法控件，不是小日历残留；`console.log` 命中集中在脚本检查器、文档示例、工具箱和工具注释，不属于小日历运行时代码调试痕迹。
needs: 用户要求做全方面收尾审查，只做必要且安全的精简整理，不引入新功能，不改变已验证的用户可见行为。核心目标是确认：小日历相关链路没有废代码、冗余分支、临时调试、职责混杂、补丁式兼容层；如果发现问题，做最小安全整理；如果没有必要修改，也不能为了“看起来做了事”去乱动稳定代码。能跑不等于能交付，但反过来，为了表演审查而改动已稳定代码，也是另一种事故源。
key_challenges: 第一，收尾审查不能变成功能扩展，尤其不能加入自动滚动、键盘操作、外部点击关闭等新行为；这些会改变用户可见交互并扩大回归面。第二，要区分真正冗余与必要防御：例如 [`setYearPickerOpen()`](modules/phone-theater/scenes/calendar.js:331) 中的 `instanceof HTMLElement` 防御看似啰嗦，但局部 patch 重渲染后 DOM 可能缺失，保留比删掉稳。第三，要区分小日历残留与项目其他合法原生 select：搜索中的 `<select` 多数来自设置页或通用表枚举字段，不应误删。第四，合同脚本本身有大量字符串检查，虽然不是完整 AST 检查，但这是项目现有合同脚本风格；收尾不应引入大范围重写。
confidence: HIGH。已读取核心实现文件和文档，搜索覆盖了旧原生下拉、编辑桥、table-generic、calendar、调试痕迹等关键字；当前没有发现必须修改的运行时代码缺陷。若后续验证失败，再按 decision_point 处理。
approach: 三维评估综合最优的方案是“审查确认 + 必要时最小整理 + 验证构建”。当前侦察未发现必须修改的废代码或破坏性冗余，因此不主动改动稳定运行时代码，只更新本轮审查缓存并运行指定验证命令。若验证或后续更细读发现具体问题，再做精确修改。
  三维评分（每个维度 1-5 分，5 为最优）：
  - 可维护性: 5/5 — 不为了表演改动而触碰已验证链路；保留 scene 内 picker 职责、通用编辑桥职责、route 分流职责和合同脚本文档职责的清晰边界。
  - 健壮性: 5/5 — 通过搜索确认旧原生年份下拉在小日历链路中无残留；保留必要 DOM 防御；继续用合同脚本、lint、build 作为门禁。
  - 可扩展性: 5/5 — `editableTables`、`table-generic:`、scene registry 与 data viewModel 仍是通用机制，后续广场/论坛/直播可以补配置而不是复制 calendar 逻辑。
edge_cases:
  - 如果搜索到 `<select` 或 `HTMLSelectElement`，必须确认是否在 calendar scene/CSS 相关链路；其他设置页或通用表合法 select 不能误删。
  - 如果发现 scene 内出现 `navigateTo` 或 `table-generic`，必须视为职责混杂；当前 calendar scene 没有直接导航。
  - 如果发现合同脚本只检查 specDoc 而不检查 architectureGuide，需要修正；当前 [`FILES`](scripts/check-theater-contract.cjs:37) 已包含 architectureGuide 并真实检查。
  - 如果发现 picker 函数重复生成按钮 HTML，需判断是否值得抽函数；当前 `renderYearPicker()` 和 `renderYearOptions()` 的重复是两段局部 HTML 字符串，但抽函数会增加间接层且不改变行为，当前不属于必须清理。
  - 如果发现 `collectDeletableKeys()` / `deleteEntities()` 返回空看似冗余，必须保留，因为 scene contract 要求每个 scene 提供删除接口，小日历通过 `deletable: false` 禁止 UI 删除。
  - 如果验证命令失败，必须重新加载技能措辞规则并输出 decision_point，不得跳过。
affected_scope:
  - [`modules/phone-theater/scenes/calendar.js`](modules/phone-theater/scenes/calendar.js)
  - [`styles/phone-theater/calendar.css`](styles/phone-theater/calendar.css)
  - [`modules/phone-theater/interactions.js`](modules/phone-theater/interactions.js)
  - [`modules/phone-core/route-renderer.js`](modules/phone-core/route-renderer.js)
  - [`modules/table-viewer/render.js`](modules/table-viewer/render.js)
  - [`modules/table-viewer/generic-viewer.js`](modules/table-viewer/generic-viewer.js)
  - [`modules/table-viewer/generic-runtime.js`](modules/table-viewer/generic-runtime.js)
  - [`modules/phone-theater/data.js`](modules/phone-theater/data.js)
  - [`modules/phone-theater/templates.js`](modules/phone-theater/templates.js)
  - [`modules/phone-theater/scenes/index.js`](modules/phone-theater/scenes/index.js)
  - [`styles/phone-theater/00-core.css`](styles/phone-theater/00-core.css)
  - [`scripts/check-theater-contract.cjs`](scripts/check-theater-contract.cjs)
  - [`docs/architecture-guide.md`](docs/architecture-guide.md)
  - [`docs/reference/theater-scene-extension-spec.md`](docs/reference/theater-scene-extension-spec.md)
  - [`dist/yuzi-phone.bundle.js`](dist/yuzi-phone.bundle.js)
  - [`dist/yuzi-phone.bundle.css`](dist/yuzi-phone.bundle.css)
  - [`.analysis-cache.md`](.analysis-cache.md)
execution_plan:
  - step_1: 创建新的 [`.analysis-cache.md`](.analysis-cache.md)，逐字记录本轮审查 analysis，避免继续使用上一轮已归档的缓存路径。
  - step_2: 若决定不修改运行时代码，也要运行用户指定验证命令 [`node scripts/check-theater-contract.cjs && npm run lint --silent && npm run build --silent`](package.json:7)，确认合同、lint 和构建仍通过。
  - step_3: 如果验证通过且无必要代码改动，进入验收，说明审查覆盖范围、确认未发现必须清理项、列出保留的“看似冗余但必要”的代码理由。
  - step_4: 如果验证失败或出现具体冗余问题，先输出 decision_point，再用编辑工具做最小修正，随后重新运行同一验证命令。
  - step_5: 验收报告追加到 [`.analysis-cache.md`](.analysis-cache.md)，再归档到 [`.analysis-archive/`](.analysis-archive/) 的独立文件，最后完成收工。
degradation_check:
  - 方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。三维评分均为 5/5；在没有发现必须修改项时，不乱改稳定代码比做无意义重构更符合生产标准。
  - 是否遗漏了已知边界条件？ → NO。已覆盖原生 select 残留、scene 内手搓跳转、route history 返回、合同脚本真实检查、调试痕迹、必要防御保留等边界。
  - 是否因改动量大而想缩减方案？ → NO。审查已覆盖核心文件和搜索关键字；不修改是基于未发现必要修改，不是回避复杂度。
  - 是否打算跳过某些文件？ → NO。affected_scope 覆盖小日历 scene、CSS、编辑桥、route、viewer、registry、data、templates、core CSS、合同脚本和文档。
  - execution_plan是否覆盖affected_scope所有文件？ → YES。step_2 的合同、lint、build 会覆盖相关源码和产物；step_3 会输出逐项审查结论；如发现问题 step_4 处理。
  - context是否充分？是否有未读但可能相关的文件？ → NO。关键链路文件已读，搜索覆盖关键字；没有发现需要额外读取的未确认依赖。
  - 是否有发现了但被我判断为"无关紧要"而跳过的问题？ → NO。唯一注意项是 picker HTML 生成有少量重复，但属于局部渲染和 patch 更新的合理重复，抽函数收益低且会增加间接层，不构成需要清理的问题。
  - execution_plan中是否有步骤计划使用 shell 命令（sed/awk/perl）修改源代码？ → NO。验证使用 shell；如需修改会使用编辑工具。
</analysis>
---
<output_quality_review>
task_summary: 本次对“小日历表 / calendar / editableTables / table-generic:<sheetKey> / 自定义年份 picker”相关实现做了全链路收尾审查。审查覆盖小剧场 scene、日历样式、通用表编辑桥、路由返回语义、合同脚本、架构文档与扩展规范。未发现需要修改的废代码、调试痕迹、原生年份下拉残留、scene 内手搓编辑跳转或破坏返回来源美化页契约的问题；因此本轮没有为了制造改动而乱动已验证代码。能不改就不改，有时候才是真正克制。乱改稳定代码，只是把“收尾”伪装成制造新风险。
deliverables:
  - 新建并维护 [`.analysis-cache.md`](.analysis-cache.md:1)，记录本轮收尾审查 analysis 与验收。
  - 审查 [`modules/phone-theater/scenes/calendar.js`](modules/phone-theater/scenes/calendar.js:277)：确认自定义年份 picker、七日窗口月历渲染、空删除接口和 `editableTables` 声明职责清晰。
  - 审查 [`styles/phone-theater/calendar.css`](styles/phone-theater/calendar.css:89)：确认无原生 select 年份样式残留，picker 面板滚动与浅色主题隔离仍在。
  - 审查 [`modules/phone-theater/interactions.js`](modules/phone-theater/interactions.js:187)：确认编辑桥统一通过 `navigateTo(\`table-generic:${sheetKey}\`)` 进入通用编辑页。
  - 审查 [`modules/phone-core/route-renderer.js`](modules/phone-core/route-renderer.js:64)：确认 `table-generic:` 分支独立于 `app:` 分支，返回语义依赖 route history 而不是 scene 私有逻辑。
  - 审查 [`modules/table-viewer/render.js`](modules/table-viewer/render.js:80)：确认 `forceGenericList` 跳过 special 检测，避免编辑桥被专属模板拦截。
  - 审查 [`scripts/check-theater-contract.cjs`](scripts/check-theater-contract.cjs:347)：确认合同覆盖禁止原生年份 select、自定义 picker、滚动样式、`editableTables`、`table-generic:<sheetKey>` 和架构文档返回语义。
  - 审查 [`docs/architecture-guide.md`](docs/architecture-guide.md:130) 与 [`docs/reference/theater-scene-extension-spec.md`](docs/reference/theater-scene-extension-spec.md:79)：确认编辑桥返回来源美化页是正式合同。
  - 运行并通过 [`node scripts/check-theater-contract.cjs && npm run lint --silent && npm run build --silent`](package.json:7)。

# 量化指标总览
metrics:
  total_files_modified: 1 — 仅创建/更新 [`.analysis-cache.md`](.analysis-cache.md:1)。运行时代码、CSS、文档、合同脚本未修改，因为审查没有发现必要改动。这个结论不是偷懒，是基于读取与搜索后的结果。
  execution_plan_coverage: 5/5 = 100% — 已完成创建缓存、运行验证、验收、准备归档；没有触发修复分支。
  edge_cases_handled: 6/6 = 100% — 已确认小日历无原生年份下拉残留、无 scene 内编辑跳转、合同脚本真实检查 architectureGuide、picker 重复 HTML 不值得抽象、空删除接口是 scene contract 必需、验证命令通过。
  confidence_assessment: HIGH — 合同、lint、build 均通过；关键文件已读；关键字搜索覆盖旧实现、编辑桥、调试痕迹与 table-generic。

# 产物实质性检查
substance_check:
  - 产物中是否存在"形式完整但实质空洞"的内容？
    → NO。本轮主要产物是审查结论与验证结果，不是新增功能。审查不是空洞，因为它实际读取了 [`calendar.js`](modules/phone-theater/scenes/calendar.js:277)、[`calendar.css`](styles/phone-theater/calendar.css:89)、[`interactions.js`](modules/phone-theater/interactions.js:187)、[`route-renderer.js`](modules/phone-core/route-renderer.js:64)、[`check-theater-contract.cjs`](scripts/check-theater-contract.cjs:347)、[`docs/architecture-guide.md`](docs/architecture-guide.md:130)、[`theater-scene-extension-spec.md`](docs/reference/theater-scene-extension-spec.md:79)，并通过搜索确认旧原生下拉与调试痕迹没有在小日历链路残留。
  - 产物是否能被其目标对象的变化所"击穿"？
    → NO。若有人把小日历重新改回原生 `<select`，[`check-theater-contract.cjs`](scripts/check-theater-contract.cjs:347) 会失败；若移除 picker 滚动样式，[`check-theater-contract.cjs`](scripts/check-theater-contract.cjs:349) 会失败；若删掉架构返回语义，[`check-theater-contract.cjs`](scripts/check-theater-contract.cjs:387) 会失败；若 scene 内错误声明不可用 role，registry 的 `editableTables role` 校验会失败。
  - 实质性比率: 1/1 = 100%。

# 覆盖完整性检查
completeness_check:
  - 是否存在被跳过的模块/函数/路径？
    → NO。审查覆盖用户点名的 scene、样式、编辑桥、路由返回、合同脚本、架构文档与扩展规范；还顺带核对了 Table Viewer 的 `forceGenericList` 入口，因为 `table-generic:<sheetKey>` 不检查这一段就等于没检查脊柱。
  - 产物覆盖的范围是否与 execution_plan 中 affected_scope 完全一致？
    → YES。affected_scope 中每个运行时代码、CSS、文档、合同脚本、dist 相关文件都通过读取、搜索或验证命令覆盖。本轮没有修改 dist 内容，但 [`npm run build --silent`](package.json:7) 已重新生成构建产物并通过。
  - 核心业务逻辑是否都有直接验证（不依赖间接覆盖）？
    → YES。核心逻辑包括：小日历禁用原生年份下拉，由 [`check-theater-contract.cjs`](scripts/check-theater-contract.cjs:347) 直接验证；picker 结构与关闭路径，由 [`check-theater-contract.cjs`](scripts/check-theater-contract.cjs:348) 验证；picker 滚动样式，由 [`check-theater-contract.cjs`](scripts/check-theater-contract.cjs:349) 验证；编辑桥返回语义，由 [`check-theater-contract.cjs`](scripts/check-theater-contract.cjs:385) 到 [`check-theater-contract.cjs`](scripts/check-theater-contract.cjs:387) 验证；语法和构建由 lint 与 build 验证。
  - affected_scope 覆盖率: 17/17 = 100%。

# 价值密度检查
value_density_check:
  - 产物中高价值内容与低价值内容的比例是多少？
    → 高价值:低价值 = 1:0，高价值占比 100%。本轮没有为了“看起来干了活”去做无意义重命名或抽象；高价值在于确认不应修改的地方确实不改，并保留已有门禁。
  - 是否存在"用数量掩盖质量"的模式——大量 trivial 产物掩盖了核心逻辑缺少验证的事实？
    → NO。没有堆产物数量；只做审查、验证、归档。核心逻辑已经由合同脚本和 build 链路覆盖。

# 需求对齐检查
alignment_check:
  - 产物满足的是用户的字面需求还是本质需求？
    → 满足本质需求。用户要求“全方面收尾审查，只做必要且安全的精简和整理，不引入新功能，不改变已验证行为”。审查结论是当前实现没有必要改动；我没有为了讨好“做点修改”的心理去破坏稳定链路。小日历表、calendar scene、自定义年份 picker、`editableTables`、`table-generic:<sheetKey>`、返回来源美化页契约均已确认。
  - "如果这是别人交给我的，我会接受吗？"
    → YES。因为他没有把收尾审查误解成二次开发，也没有把“无必要改动”粉饰成“偷懒”。验证命令通过，合同脚本能击穿主要回归点，保留的看似冗余项都有合理职责：`collectDeletableKeys()` / `deleteEntities()` 是 scene contract，DOM `instanceof HTMLElement` 是局部 patch 防御，picker HTML 少量重复是避免过度抽象。
</output_quality_review>
