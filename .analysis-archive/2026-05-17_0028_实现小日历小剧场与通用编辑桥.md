<analysis>
context: 本轮侦察确认了以下事实：1) 路由入口在 `modules/phone-core/route-renderer.js` 的 `loadRouteRenderer(route, renderToken)` 中分发；普通表只识别 `app:`，在第 37-44 行取 `sheetKey = route.replace('app:', '')` 后调用 `renderTableViewer(page, sheetKey)`；小剧场通过 `isTheaterRoute(route)` 与 `renderTheaterScene(page, sceneId, { renderToken })` 分发。2) 表格查看入口在 `modules/table-viewer/render.js` 的 `renderTableViewer(container, sheetKey)`，它创建 `viewerRuntime` 后解析 `viewerContext`，第 75-81 行先检测 `detectSpecialTemplateForTable` 与 `detectSpecialTableType`，命中后进入 special runtime；未命中才进入 `renderGenericTableViewer`。因此要实现“编辑按钮进入原始通用列表”，必须给 `renderTableViewer` 增加 `options.forceGenericList` 并跳过 special 检测，否则未来任何专属表都会再次拦截编辑入口。3) 通用表 runtime 在 `modules/table-viewer/generic-runtime.js` 的 `createGenericTableViewerRuntime(container, context, hooks)`，第 114 行创建 `createTableViewerState(sheetKey)`，状态默认 `mode: 'list'`，`returnToListMode()` 可清理详情态；目前没有外部 `initialMode` 参数。因为每次 `renderTableViewer` 都新建 runtime，强制列表主要需要跳过 special；但为了防止后续状态复用引入回归，可在 `forceGenericList` 下显式调用 `runtime.state.returnToListMode()` 或给 hooks 增加 `initialMode: 'list'`。4) 小剧场 scene registry 在 `modules/phone-theater/scenes/index.js`，当前只导入并注册 `squareScene`、`forumScene`、`liveScene`；`normalizeSceneDefinition` 会校验 `id/appKey/route/primaryTableRole/tables/buildViewModel/renderContent/collectDeletableKeys/deleteEntities`，并冻结 child table 关系。这里需要新增 `calendarScene`，并增加 `editableTables` 归一化与 role 校验。5) 小剧场数据层在 `modules/phone-theater/data.js`，`resolveTheaterSceneTables` 会把 scene 的 role 映射到实际表对象并提供 `childSheetKeys`；`buildTheaterSceneViewModel` 当前只返回 `content/tables/childSheetKeys`，没有编辑入口数据。这里应把 `scene.editableTables` 解析成 `{ role, label, description, sheetKey, tableName, available }`，供 shell 与 interactions 使用。6) 小剧场 shell 在 `modules/phone-theater/templates.js`，`renderNav(title, uiState)` 固定右上删除按钮 `phone-theater-delete-toggle`；小日历要求不显示删除、显示编辑，因此需要把 nav 改为根据 `uiState.canDelete` 与 `uiState.editableTables` 渲染工具栏，而不是硬编码 scene 分支。7) 小剧场交互在 `modules/phone-theater/interactions.js`，容器 click 监听目前只调用 `handleDeleteAction`；需要新增编辑 action 处理。单表 `editableTables` 直接 `navigateTo('table-generic:<sheetKey>')`，多表需要一个轻量菜单状态。该文件当前没有导入 `navigateTo`，需要新增导入。8) scene 专属交互通过 `scene.bindInteractions(container, context)` 接入，直播已经用它处理弹幕按钮；小日历的月份切换、年份选择、日期选中应放在 `calendarScene.bindInteractions`，而不是塞进核心 interactions。9) 样式入口在 `styles/phone-theater/index.css`，按 core、square、forum、live 顺序 import；新增 calendar.css 需要登记。核心 CSS `styles/phone-theater/00-core.css` 只放通用 shell、删除态、通用标签与空态；新增编辑按钮和编辑菜单属于通用 shell，可放 core；小日历网格样式必须放 `styles/phone-theater/calendar.css` 并以 `[data-theater-scene="calendar"]` 作用域开头。10) 小日历表源 `tables/sources/小剧场2.1/10-小日历表.md` 已存在，`uid: sheet_small_calendar`、`name: 小日历表`、`orderNo: 9`，字段为 日期、星期几、节日、大事件、状态、与今天的关系、内容、月份几天。`npm run tables:check` 已执行通过，输出 `mate 1 个，sheet 10 个`。11) 契约检查脚本 `scripts/check-theater-contract.cjs` 当前把 SCENES 写死为 square/forum/live，并检查 registry 导入三个内置 scene、style registry 顺序、core 文件禁止 scene 分支等。新增 calendar scene 后必须更新 FILES 与 SCENES，否则合同脚本会看不到新 scene，也可能仍只验证旧顺序。12) 现有 `dist/yuzi-phone.bundle.js` 是构建产物，本轮不手工改 dist，应由 `npm run build` 生成。
needs: 用户的本质目标是把“小日历表”从表源推进到可用的小手机美化 App：导入表后首页出现小日历小剧场入口；点击显示手机月历网格；前端派生月份天数和星期几；今天标红；支持左右切换月份和年份选择；点击日期在下方显示该日 7 行窗口中的节日、大事件、状态与内容；右上角编辑进入原始通用表列表。工程目标不是只让小日历能跑，而是建立后续广场、论坛、直播也能复用的“美化页 → 原始通用表列表”桥，因此实现必须沉淀为 `table-generic:<sheetKey>` 路由、`forceGenericList`、scene `editableTables` 契约和小剧场 shell 通用编辑入口。
key_challenges: 1) route 语义不能污染现有 `app:`：如果用 `app:${sheetKey}?mode=generic`，会破坏现有 `route.replace('app:', '')` 的简单解析，还会让 `app:` 兼具普通表、美化表、强制通用列表多种语义。2) `renderTableViewer` 必须跳过 special 检测，否则编辑入口可能被 special runtime 或未来小日历专属识别重新拦截。3) 小剧场 shell 目前固定删除按钮，直接为 calendar 写 sceneId 分支会违反 theater 扩展规范；必须用 `canDelete/canEdit/editableTables` 等通用 UI state。4) 多表 scene 的编辑入口未来需要弹菜单，因此第一阶段即使小日历是单表，也要把 `editableTables` 数据结构设计成数组，不能写成 `editSheetKey` 单值。5) 小日历日期解析要同时处理现实 `YYYY-MM-DD` 和抽象三段式；抽象日期只能稳定锚定用于展示，不应伪装为剧情事实。6) 内容渲染必须全量转义，不能把表格用户内容直接拼进 HTML。7) 契约脚本写死旧 scene，新增 scene 后如果不更新检查，会让“看起来能跑”但缺合同覆盖；这是未来事故入口。
confidence: HIGH
  - HIGH: 侦察覆盖了 route、table viewer、generic runtime、theater registry、data viewModel、templates、interactions、style registry、core CSS、scene 示例、表源和 theater contract 脚本；核心调用链和修改点明确。唯一未验证的是浏览器视觉效果，但代码层面的影响范围已经足够明确，后续可通过 lint/build/contract 以及必要的浏览器检查补齐。
approach: 三维评估综合最优的方案是按计划实现“小剧场 scene + 通用编辑桥”：新增 `table-generic:<sheetKey>` 路由，扩展 `renderTableViewer(container, sheetKey, options = {})` 支持 `forceGenericList`；给 scene registry 增加 `editableTables` 契约；在 data 层解析编辑入口；在 templates/interactions 中提供通用编辑按钮和多表菜单；新增 calendar scene 与 calendar.css。三维评分：
  - 可维护性: 5/5 — route、viewer、scene 契约、shell、scene 渲染各司其职；小日历交互放 scene hook，编辑桥放核心通用层，避免在 calendar scene 内手搓跳转，也避免在 Table Viewer 里写小剧场专属逻辑。
  - 健壮性: 4/5 — 方案能处理 missing sheetKey、editableTables role 不存在、单表/多表入口、special 拦截、route history、用户内容转义、抽象日期降级；浏览器视觉和跨月抽象日期仍需验收验证，所以不是 5/5。
  - 可扩展性: 5/5 — 后续广场/论坛/直播只需补 `editableTables` 配置即可接入同一桥；新增小剧场也沿用 scene module、style file、registry、contract 的既有模式。
edge_cases: 1) `editableTables` 声明了不存在于 `scene.tables` 的 role 时，registry 应在启动时抛错，而不是运行时按钮点击失败。2) `editableTables` 对应表在当前 rawData 缺失时，viewModel 应返回 unavailable 项，UI 隐藏或禁用该项，点击不能报错。3) 单表小日历点击编辑应直接进入 `table-generic:<sheetKey>`；多表 scene 后续点击编辑应打开菜单，不应常驻多个按钮挤爆导航栏。4) `forceGenericList` 为 true 时必须跳过 `detectSpecialTemplateForTable` 与 `detectSpecialTableType`，否则编辑桥会被 special 拦截。5) 强制通用入口必须确保列表模式，不能残留详情态、编辑态、删除态或字段锁管理态。6) 小日历表缺少 `今天` 行时，calendar scene 应用第一行有效日期或当前日期降级，页面不能空白或抛错。7) 日期为抽象三段式如 `丰收纪-丰收月-12` 时，必须用最后一段数字日和稳定哈希月份锚定展示；不能把抽象星期几当剧情事实。8) 月末跨月和月初回退要依靠 JS Date 生成网格，不能拼字符串生成 `05-34`。9) 用户内容字段包含 HTML、引号或换行时必须通过 `escapeHtml`/`escapeHtmlAttr` 转义。10) 小日历不需要删除按钮，因此 `canDelete` false 时删除管理条和删除按钮都不能出现。11) scene 专属交互重复渲染后不能重复绑定导致点击一次触发多次；应通过生命周期 `addEventListener` 或容器重建自然清理。12) `scripts/check-theater-contract.cjs` 新增 calendar 后必须同步 FILES、SCENES、style order 检查，否则合同覆盖不完整。
affected_scope: `tables/sources/小剧场2.1/10-小日历表.md`; `tables/generated/小剧场2.1.json`; `modules/phone-core/route-renderer.js`; `modules/table-viewer/render.js`; `modules/table-viewer/generic-viewer.js`; `modules/table-viewer/generic-runtime.js`; `modules/phone-theater/scenes/index.js`; `modules/phone-theater/data.js`; `modules/phone-theater/templates.js`; `modules/phone-theater/interactions.js`; `modules/phone-theater/scenes/calendar.js`; `styles/phone-theater/index.css`; `styles/phone-theater/00-core.css`; `styles/phone-theater/calendar.css`; `scripts/check-theater-contract.cjs`; `docs/reference/theater-scene-extension-spec.md`; `.analysis-cache.md`; `.analysis-archive/*`.
execution_plan:
  - step_1: 运行 `npm run tables:build`，生成 `tables/generated/小剧场2.1.json`，确保已经通过的 `10-小日历表.md` 进入合成产物；此步骤只生成表 JSON，不修改运行时代码。
  - step_2: 修改 `modules/phone-core/route-renderer.js`，新增常量或直接识别 `table-generic:` route prefix，在该分支动态导入 `renderTableViewer` 并调用 `renderTableViewer(page, sheetKey, { forceGenericList: true })`；保留现有 `app:` 与 `theater:` 语义不变。
  - step_3: 修改 `modules/table-viewer/render.js`，把 `renderTableViewer(container, sheetKey)` 扩展为 `renderTableViewer(container, sheetKey, options = {})`；创建 viewerRuntime 时让 `rerenderViewer` 保留 options；当 `options.forceGenericList === true` 时跳过 special 检测并直接进入 generic；普通路径完全保留原 special/generic 分流。
  - step_4: 修改 `modules/table-viewer/generic-viewer.js` 与 `modules/table-viewer/generic-runtime.js`，透传 hooks，并在 hooks.initialMode 为 `list` 或 hooks.forceListMode 为 true 时显式调用 `state.returnToListMode()`，保证强制通用入口不会显示详情态。
  - step_5: 修改 `modules/phone-theater/scenes/index.js`，导入 `calendarScene`，把它加入 `RAW_THEATER_SCENES`，并在 `normalizeSceneDefinition` 中归一化 `editableTables`：过滤文本、校验 role 属于 `tables`、冻结对象；`getTheaterSceneDefinitions` 也要复制 editableTables 供外部读取。
  - step_6: 修改 `modules/phone-theater/data.js`，新增内部 `buildEditableTableEntries(scene, tables)`，在 `buildTheaterSceneViewModel` 的可用与不可用返回中提供 `editableTables`；每项包含 role、label、description、tableName、sheetKey、available。
  - step_7: 修改 `modules/phone-theater/render.js` 与 `modules/phone-theater/templates.js`，在 `buildUiState` 中加入 `canDelete`、`editableTables`、`editMenuOpen` 等状态；`renderNav` 改成通用工具栏：按 `canDelete` 显示删除按钮，按 editableTables 显示编辑按钮；多表时渲染编辑菜单。
  - step_8: 修改 `modules/phone-theater/interactions.js`，导入 `navigateTo`，新增编辑 action 处理：单表直接导航到 `table-generic:${sheetKey}`；多表切换菜单；菜单项点击按对应 sheetKey 导航；缺 sheetKey 时 toast 错误并不导航。保留删除链路与 scene.bindInteractions hook。
  - step_9: 新增 `modules/phone-theater/scenes/calendar.js`，实现 scene metadata、`tables: { days: '小日历表' }`、`editableTables`、`fieldSchema`、`contract`、`buildViewModel`、空删除能力、`renderContent`、`bindInteractions`；ViewModel 负责读取七日窗口、解析现实/抽象日期、生成 month grid、标记 today/selected/hasContent，并渲染下方内容。
  - step_10: 修改 `styles/phone-theater/00-core.css`，加入通用 nav actions、编辑按钮、编辑菜单样式；不得引用 calendar 专属容器类。
  - step_11: 新增 `styles/phone-theater/calendar.css`，以 `.phone-theater-page[data-theater-scene="calendar"]` 作用域实现月历页面、月份控制、星期栏、日期格、今天标红、有内容日、非当前月弱化、内容面板样式。
  - step_12: 修改 `styles/phone-theater/index.css`，在 live 后导入 `calendar.css`；如 contract 要求顺序，则同步更新检查脚本。
  - step_13: 修改 `scripts/check-theater-contract.cjs`，把 calendarScene/calendarCss 加入 FILES 与 SCENES，增加 calendar 必需 class、表名、role、无删除 role 的支持、style registry 顺序检查、editableTables 契约检查、routeRenderer 对 `table-generic:` 的检查，以及 templates/interactions 对编辑入口的检查。
  - step_14: 修改 `docs/reference/theater-scene-extension-spec.md`，补充可选 `editableTables` 契约、单表直接跳转、多表菜单、`table-generic:<sheetKey>` 强制通用列表桥，避免文档落后于实现。
  - step_15: 运行 `npm run tables:check`、`npm run tables:build`、`node scripts/check-theater-contract.cjs`、`npm run lint --silent`、`npm run build --silent`；若失败，按错误重新加载技能并输出 decision_point 后修正。
  - step_16: 执行验收：读取 `.analysis-cache.md`，输出 `output_quality_review`，追加写入缓存并归档到 `.analysis-archive/`。
degradation_check:
  - 方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。可维护性 5/5，因为通用桥、scene 契约、shell、scene 渲染分层清楚；健壮性 4/5，因为覆盖主要边界但仍需浏览器视觉验收；可扩展性 5/5，因为后续多表 scene 只补 editableTables 配置即可复用。
  - 是否遗漏了已知边界条件？ → NO。已列出 role 无效、sheet 缺失、单表/多表、special 拦截、列表态清理、今天缺失、抽象日期、跨月、转义、删除按钮隐藏、重复绑定、contract 更新等边界。
  - 是否因改动量大而想缩减方案？ → NO。改动涉及多文件，但这是通用桥必须覆盖的完整范围；缩成 calendar scene 内部跳转会损害后续广场/论坛/直播复用。
  - 是否打算跳过某些文件？ → NO。affected_scope 覆盖 route、viewer、generic runtime、scene registry、data、templates、interactions、calendar scene、CSS、contract、文档、表源生成与分析归档。
  - execution_plan是否覆盖affected_scope所有文件？ → YES。每个 affected_scope 文件都对应 step_1 到 step_16 的具体处理或产物生成。
  - context是否充分？是否有未读但可能相关的文件？ → NO。已读取核心调用链和代表 scene/CSS/contract；dist 是构建产物，不手工读写。若后续 lint/build 暴露隐藏依赖，再通过 decision_point 处理。
  - 是否有发现了但被我判断为"无关紧要"而跳过的问题？ → NO。已将 contract 脚本写死旧 scene、shell 固定删除按钮、special 拦截和抽象日期都纳入执行计划。
  - execution_plan中是否有步骤计划使用 shell 命令（sed/awk/perl）修改源代码？ → NO。修改源码全部使用编辑工具；shell 只用于表源合成、contract、lint、build 等验证命令。
</analysis>
---
<decision_point>
issue: 运行 `npm run tables:check && npm run tables:build && node scripts/check-theater-contract.cjs && npm run lint --silent && npm run build --silent` 时，表源校验、表源合成和 theater contract 均通过，但 lint 在 `modules/phone-theater/scenes/calendar.js` 第 8 行报错：`WEEKDAY_LABELS is assigned a value but never used`。这个问题在侦察/分析阶段没有预见到，因为当时计划了星期几派生能力，但实际实现渲染使用的是 `WEEKDAY_HEADINGS`，`WEEKDAY_LABELS` 在最终代码路径中没有被消费。
impact: YES。该问题阻断 `npm run lint --silent`，因此阻断 `npm run build --silent` 和最终交付。影响范围限定在 `modules/phone-theater/scenes/calendar.js` 的无用常量，不影响已通过的表源合成与 theater contract。
context_update: `execution_plan` 的 step_15 需要追加 lint 修复后重跑验证；`edge_cases` 不变；`affected_scope` 保持包含 `modules/phone-theater/scenes/calendar.js`；`confidence` 仍为 HIGH，因为这是明确的静态检查错误。
options:
  - option_a:
      description: 删除未使用的 `WEEKDAY_LABELS` 常量，只保留实际用于星期栏渲染的 `WEEKDAY_HEADINGS`。
      approach_evaluation: 可维护性 5/5，删除死代码后意图更清晰；健壮性 5/5，不改变运行时行为；可扩展性 5/5，未来如果需要完整星期标签可再按实际用途添加，当前不保留误导性常量。
      edge_cases: 无新增运行时边界；需要确保没有任何代码引用 `WEEKDAY_LABELS`。
      affected_scope_delta: 仅修改 `modules/phone-theater/scenes/calendar.js`。
  - option_b:
      description: 把 `WEEKDAY_LABELS` 改名为 `_WEEKDAY_LABELS` 以满足 ESLint 未使用变量忽略规则。
      approach_evaluation: 可维护性 2/5，保留无用常量会误导维护者以为它有协议用途；健壮性 5/5，不改变运行时行为；可扩展性 2/5，未来维护者可能继续沿用无意义常量。
      edge_cases: 无新增运行时边界，但会制造“为什么存在未使用星期标签”的理解成本。
      affected_scope_delta: 仅修改 `modules/phone-theater/scenes/calendar.js`。
  - option_c:
      description: 在渲染中改用 `WEEKDAY_LABELS`，例如把星期栏变成 `日、一、二、三、四、五、六`。
      approach_evaluation: 可维护性 3/5，会引入与当前 `startOfWeek = 1` 月历算法不一致的星期栏顺序；健壮性 2/5，星期栏会从周日开始但网格从周一开始，用户看到的列会错位；可扩展性 2/5，后续要修正周起始规则更麻烦。
      edge_cases: 会引入星期栏与 grid 起始日错位问题，尤其当前 `buildMonthGrid(year, monthIndex, 1)` 是周一开头。
      affected_scope_delta: 修改 `modules/phone-theater/scenes/calendar.js` 的渲染字段，可能还要调整 grid 起始规则。
recommendation: 选择 option_a。三维评估显示 option_a 在可维护性 5/5、健壮性 5/5、可扩展性 5/5 上综合最优；option_b 是保留死代码的伪修复，option_c 会引入星期栏错位风险。
execution_plan_update: 在原 step_15 中插入修复动作：删除 `modules/phone-theater/scenes/calendar.js` 中未使用的 `WEEKDAY_LABELS` 常量，然后重新运行 `npm run lint --silent` 与后续完整验证命令。
deviation_audit:
  original_plan_excerpt: `- step_15: 运行 `npm run tables:check`、`npm run tables:build`、`node scripts/check-theater-contract.cjs`、`npm run lint --silent`、`npm run build --silent`；若失败，按错误重新加载技能并输出 decision_point 后修正。`
  current_proposal: 删除 `modules/phone-theater/scenes/calendar.js` 中未使用的 `WEEKDAY_LABELS` 常量，然后重新运行验证命令。
  diff_summary: 新增一个 lint 修复动作；不删除任何原验证步骤；不改变架构方案；不改变 calendar scene 行为。
  deviation_motive_check:
    - 措辞替换规则逐类检查：全部未命中。推荐理由没有使用捷径导向、放弃正确方案、虚假完成、过度抽象、自我降权、逃避复杂度、内部省略、工具降级、伪理性化或执行顺序降级措辞。
    - 偏离后的方案在三维评分上是否低于原方案？→ NO。删除死代码提高可维护性，不降低健壮性和可扩展性。
    - 偏离是否导致 affected_scope 缩小？→ NO。仍覆盖原 affected_scope，只是在 `calendar.js` 内修正 lint。
  self_dissection: 未触发。没有缩小范围、降低质量、回避复杂度或急于收尾。
degradation_check:
  - 推荐方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。option_a 三维均为 5/5。
  - 推荐方案是否遗漏了新发现的边界条件？ → NO。该 lint 问题没有运行时边界，只需确认无引用。
  - 是否因为想尽快完成而选择了改动量小的方案？ → NO。选择删除死代码是因为它三维最优，不是因为代码最少。
  - 修改后的execution_plan是否仍覆盖所有affected_scope？ → YES。仅在 `calendar.js` 内修复 lint，原验证步骤继续执行。
  - 是否有发现了但被判断为"无关紧要"而跳过的问题？ → NO。lint error 阻断交付，必须修复。
  - options 是否包含至少三个方案？ → YES。已列出 option_a、option_b、option_c。
  - 是否因为工具报错而准备换用 shell 命令修改源代码？ → NO。将使用编辑工具删除常量。
  - deviation_audit 是否触发了 self_dissection？ → NO。
</decision_point>
---
<output_quality_review>
task_summary: 本次任务把“小日历表”从表格事实源推进到小手机可用的小剧场美化入口：创建并合成小日历表源，新增小日历 calendar scene，建立 `table-generic:<sheetKey>` 强制通用表列表编辑桥，扩展小剧场 `editableTables` 契约与通用编辑按钮，并用契约脚本、表源校验、lint 与 build 验证交付结果。
deliverables: 本轮直接交付的文件包括：`tables/sources/小剧场2.1/10-小日历表.md`、`tables/generated/小剧场2.1.json`、`plans/small-calendar-table-plan.md`、`modules/phone-core/route-renderer.js`、`modules/table-viewer/render.js`、`modules/table-viewer/generic-runtime.js`、`modules/phone-theater/scenes/index.js`、`modules/phone-theater/data.js`、`modules/phone-theater/render.js`、`modules/phone-theater/templates.js`、`modules/phone-theater/interactions.js`、`modules/phone-theater/scenes/calendar.js`、`styles/phone-theater/00-core.css`、`styles/phone-theater/index.css`、`styles/phone-theater/calendar.css`、`scripts/check-theater-contract.cjs`、`docs/reference/theater-scene-extension-spec.md`、`dist/yuzi-phone.bundle.js`、`dist/yuzi-phone.bundle.css`、`dist/yuzi-phone.bundle.js.map`、`dist/yuzi-phone.bundle.css.map`，以及归档用分析记录。

# 量化指标总览
metrics:
  total_files_modified: 22 — 21 个功能、文档、表源或构建产物文件，加 1 个分析归档文件。
  execution_plan_coverage: 16/16 = 100% — 表源合成、路由桥、viewer 强制通用入口、scene registry、data viewModel、shell 编辑入口、calendar scene、样式、契约脚本、文档、验证与归档步骤均已执行。
  edge_cases_handled: 12/12 = 100% — role 校验、sheet 缺失降级、单表直接编辑、多表菜单结构、special 拦截绕过、列表态复位、今天缺失降级、抽象日期解析、跨月网格、用户内容转义、删除按钮隐藏、重复绑定风险均已在实现或契约中处理。
  confidence_assessment: MEDIUM — 静态合同、表源合成、lint 和 build 全部通过，但没有在真实 SillyTavern 浏览器会话中进行人工点击与视觉验收。
    - MEDIUM: 存在 1 个未验证假设：calendar scene 在宿主真实页面内的视觉尺寸、点击选择日期、月份切换和年份选择交互未经过浏览器人工验证。代码层面已通过 `node scripts/check-theater-contract.cjs`、`npm run lint --silent` 和 `npm run build --silent`，因此不属于未完成实现，但置信度不能伪装成 HIGH。

# 产物实质性检查
substance_check:
  - 产物中是否存在"形式完整但实质空洞"的内容？
    → NO。`10-小日历表.md` 定义了实际表字段、DDL 约束和 AI 填表规则；`calendar.js` 实现日期解析、月历网格、内容映射和交互；route/viewer/runtime 修改让编辑桥真实绕过专属视图；theater registry/data/templates/interactions 修改让 `editableTables` 进入真实 UI 与导航链路；CSS 文件提供实际布局；contract 脚本会检查 scene、样式、route 和文档同步；dist 文件由构建产出并承载浏览器运行代码。
  - 产物是否能被其目标对象（被测代码/被重构模块/被修复的bug）的变化所"击穿"？
    → YES。删除 calendar scene 注册、删掉 `table-generic` route、移除 `editableTables` 解析、漏导入 `calendar.css`、移除 calendar 必需 class、破坏表源 JSON 块或写错 orderNo，都会被 `npm run tables:check`、`npm run tables:build`、`node scripts/check-theater-contract.cjs`、`npm run lint --silent` 或 `npm run build --silent` 击穿。浏览器视觉细节不能被当前静态检查完全击穿，因此置信度维持 MEDIUM。
  - 实质性比率: 22/22 = 100% — 每个交付文件都改变了表源、运行时行为、样式、契约、构建产物或决策追溯能力；没有只为凑数量存在的空文件。

# 覆盖完整性检查
completeness_check:
  - 是否存在被跳过的模块/函数/路径？
    → NO。核心路径覆盖了首页小剧场发现、theater scene 注册、viewModel 构建、shell nav 渲染、编辑按钮交互、通用表强制入口、calendar scene 自身渲染与样式。`modules/table-viewer/generic-viewer.js` 在 affected_scope 中被评估过，实际执行确认现有入口已经能透传 runtime 依赖，最终落点集中在 `render.js` 和 `generic-runtime.js`，没有为了回避困难而跳过。
  - 产物覆盖的范围是否与 execution_plan 中 affected_scope 完全一致？
    → YES。affected_scope 中列出的表源、生成物、route、viewer、generic runtime、theater registry、data、templates、interactions、calendar scene、CSS、contract、文档和分析归档均已处理。`generic-viewer.js` 没有最终 diff，是因为现有 `renderGenericTableViewer` 结构无需修改即可承接新增 hooks，不是漏改。
  - 核心业务逻辑是否都有直接验证（不依赖间接覆盖）？
    → YES。表源结构由 `npm run tables:check` 直接验证；合成结果由 `npm run tables:build` 直接生成；小剧场 scene、style registry、editableTables 和 `table-generic` route 由 `node scripts/check-theater-contract.cjs` 直接验证；语法和未使用变量由 `npm run lint --silent` 直接验证；最终 bundle 由 `npm run build --silent` 直接验证。真实浏览器点击属于运行时人工验收，当前没有直接验证，已反映在 confidence_assessment。
  - affected_scope 覆盖率: 18/18 = 100% — affected_scope 中所有文件或路径均被修改、生成、评估或归档。

# 价值密度检查
value_density_check:
  - 产物中高价值内容（验证核心逻辑/处理复杂场景）与低价值内容（验证trivial行为）的比例是多少？
    → 高价值:低价值 = 17:5，高价值占比 77%。高价值内容包括表源约束、route 桥、viewer 强制通用入口、scene registry、data 解析、shell 编辑入口、interactions 导航、calendar scene、calendar CSS、contract 脚本和规范文档；低价值内容主要是构建生成的 map 文件与追溯记录，它们不是核心逻辑，但对发布和排查仍有作用。
  - 是否存在"用数量掩盖质量"的模式——大量 trivial 产物掩盖了核心逻辑缺少验证的事实？
    → NO。核心逻辑集中在 route/viewer/theater/calendar/contract，而不是靠 dist 或 map 文件堆数量。验证命令覆盖了表源、合同、lint 和 bundle；未做浏览器人工点击这一点已经明确降低置信度，没有粉饰成完全人工验收。

# 需求对齐检查
alignment_check:
  - 产物满足的是用户的字面需求还是本质需求？
    → 满足本质需求。用户不是只要一张表，而是要小手机中可持续扩展的小日历美化 App，并为后续广场、论坛、直播等小剧场建立统一编辑链路。本轮交付同时完成小日历表源、小日历美化 scene、月历网格、月份/年份切换、今天标红、有内容日期详情、右上编辑进入原始通用表列表，以及后续 scene 可复用的 `editableTables` 契约和 `table-generic` 桥。
  - "如果这是别人交给我的，我会接受吗？"
    → YES，但会附带验收备注：代码结构、契约覆盖、表源合成、lint 和 build 达到可交付标准；真实浏览器视觉与点击链路仍应在 SillyTavern 宿主里做一次人工验收。这个备注不是把问题留给用户，而是明确当前验证边界，避免把静态验证伪装成端到端人工验证。能跑不等于能交付，别把临时拼起来的东西当成果；本轮至少没有犯这种低级错误。

→ 验收未发现需要就地修正的代码问题。唯一未覆盖的是宿主浏览器人工点击验收，属于当前环境未执行的验证层级，已经体现在 MEDIUM 置信度和最终汇报中。
</output_quality_review>
