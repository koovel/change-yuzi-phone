<analysis>
context: 本次收尾审查发生在用户要求把 [`测试1.2.json`](测试1.2.json) 的纪要表和小日历表正式同步进 `tables` 并合成之后。已确认本轮直接产物是 [`tables/sources/纪要/01-纪要表.md`](tables/sources/纪要/01-纪要表.md:1)、[`tables/sources/小剧场2.1/10-小日历表.md`](tables/sources/小剧场2.1/10-小日历表.md:1)、[`tables/generated/纪要.json`](tables/generated/纪要.json:1)、[`tables/generated/小剧场2.1.json`](tables/generated/小剧场2.1.json:1)。读取表源后确认纪要表包含 `与今天的关系` 字段，且在 [`tables/sources/纪要/01-纪要表.md`](tables/sources/纪要/01-纪要表.md:31) 明确为前端派生字段；DDL 在 [`tables/sources/纪要/01-纪要表.md`](tables/sources/纪要/01-纪要表.md:75) 增加 `today_relation TEXT`，content 在 [`tables/sources/纪要/01-纪要表.md`](tables/sources/纪要/01-纪要表.md:91) 包含“与今天的关系”。小日历表源在 [`tables/sources/小剧场2.1/10-小日历表.md`](tables/sources/小剧场2.1/10-小日历表.md:43) 强化固定 7 行、唯一关系、删除重建、继承旧内容和 SQL 禁注释规则；DDL 在 [`tables/sources/小剧场2.1/10-小日历表.md`](tables/sources/小剧场2.1/10-小日历表.md:70) 保留 `date_text` 唯一，在 [`tables/sources/小剧场2.1/10-小日历表.md`](tables/sources/小剧场2.1/10-小日历表.md:74) 限制 `day_status` 枚举，在 [`tables/sources/小剧场2.1/10-小日历表.md`](tables/sources/小剧场2.1/10-小日历表.md:75) 限制 `today_relation` 唯一枚举。读取 generated 文件确认 [`tables/generated/纪要.json`](tables/generated/纪要.json:19) 含 `sheet_summary`，[`tables/generated/小剧场2.1.json`](tables/generated/小剧场2.1.json:648) 含 `sheet_small_calendar`，且其字段与 source 同步。执行 `git diff --name-status` 与 `git diff --stat` 后发现工作区还有大量既有未提交改动和未跟踪目录，例如 [`modules/phone-core/date-relation.js`](modules/phone-core/date-relation.js:1)、[`modules/phone-core/derived-fields/chronicle-today-relation.js`](modules/phone-core/derived-fields/chronicle-today-relation.js:1)、[`modules/phone-theater/scenes/calendar.js`](modules/phone-theater/scenes/calendar.js:1)、[`dist/yuzi-phone.bundle.js`](dist/yuzi-phone.bundle.js:1) 等；这些属于前面功能开发或既有工作区状态，不应在本次“表源同步收尾清理”里扩大修改。搜索 `tmp-sync|console.log|debugger|<<<<<<<|>>>>>>>|TODO|FIXME` 未发现本轮临时脚本 `scripts/tmp-sync-test-sheets.cjs` 残留；搜索结果中的 `console.log` 主要位于构建脚本、合同检查脚本、文档样例、第三方/工具箱目录和打包产物，不是本轮表源同步引入的调试残留。
needs: 用户要的是严格收尾审查与清理：确认 1617 行相关改动中没有临时残留、无用代码、冗余逻辑、重复内容或会阻碍维护的实现；若发现确认无用或冗余的内容就直接删除或精简；若未发现可安全删除内容，则不为了“看起来清理过”而乱改。最终必须重新校验表源和生成物，确保功能与生成结果不受影响，并以“完美收工”结尾。
key_challenges: 第一，当前工作区存在大量非本轮目标改动，如果把它们一起清理就是扩大范围，事故味太浓。第二，generated JSON 是由 source 合成的产物，不能在 generated 中手工“精简提示词”，否则下一次 build 会被覆盖，维护链路会分裂。第三，小日历提示词看起来长，但其中状态枚举、唯一冲突删除重建、旧内容继承、SQL 禁注释都是针对用户实际 shujuku 报错的必要防护，不是冗余。第四，纪要表中的“AI 不填写 today_relation”在 note、initNode、insertNode 中重复出现，但这是提示词分区的必要约束传递，不属于可删重复；删掉任一处会让不同生成节点漏约束。
confidence: HIGH
  - 已读取四个直接产物，并读取相关前端派生字段实现用于判断表源规则是否必要。
  - 已执行工作区 diff/status 侦察，确认存在非本轮改动，必须避免越界清理。
  - 已搜索常见临时残留和冲突标记，未发现本轮临时脚本残留或冲突标记。
approach: 三维评估综合最优的方案是“只清理经证据确认属于本轮且无用的内容；否则保持产物不动并进行校验”。不是为了满足“清理”两个字就删提示词，那是把生产约束当装饰品，幼稚得很。表源中的长提示词是为约束 AI SQL 行为服务的，属于业务合同。若审查没有发现确认无用部分，正确动作是记录“未删改”，然后跑 check/build 验证。
  三维评分（每个维度 1-5 分，5 为最优）：
  - 可维护性: 5/5 — 保留 source -> generated 的单一事实源链路，不手改 generated，不扩大到非目标模块。
  - 健壮性: 5/5 — 保留针对唯一约束、枚举约束、继承旧内容和 SQL 注释污染的防护文本，并用 check/build 复验。
  - 可扩展性: 4/5 — 未来可继续按同一标准审查表源；不足是当前表源系统没有“按 sheet diff 审查”的专用脚本，只能结合读取和 build 验证。
edge_cases:
  - 不能删除小日历表中“删除旧 7 行再插入完整 7 行”的规则，因为它直接对应 `date_text` 和 `today_relation` 唯一约束冲突。
  - 不能删除“按日期继承仍在新窗口内旧内容”的规则，否则会回到用户明确担心的旧日期内容被重写问题。
  - 不能删除“状态只能填写 上班/上学 或 放假”的重复提示，因为它对应 `day_status` CHECK 失败。
  - 不能删除“SQL 禁止注释”的重复提示，因为用户日志已经出现 SQL 注释污染。
  - 不能手工精简 [`tables/generated/纪要.json`](tables/generated/纪要.json:1) 或 [`tables/generated/小剧场2.1.json`](tables/generated/小剧场2.1.json:1)，generated 必须由 source 生成。
  - 不能把工作区中其他历史改动、打包产物或第三方工具目录纳入本次清理。
  - 如果没有发现确认无用内容，应不做代码改动；硬删必要约束只会制造回归。
affected_scope:
  - [`tables/sources/纪要/01-纪要表.md`](tables/sources/纪要/01-纪要表.md)
  - [`tables/sources/小剧场2.1/10-小日历表.md`](tables/sources/小剧场2.1/10-小日历表.md)
  - [`tables/generated/纪要.json`](tables/generated/纪要.json)
  - [`tables/generated/小剧场2.1.json`](tables/generated/小剧场2.1.json)
  - 工作区临时残留检查：[`scripts/tmp-sync-test-sheets.cjs`](scripts/tmp-sync-test-sheets.cjs) 不应存在
execution_plan:
  - step_1: 对四个直接产物逐段审查，确认字段、DDL、提示词、配置、生成物结构是否必要且一致。
  - step_2: 搜索临时脚本、调试输出、冲突标记、TODO/FIXME，确认是否存在本轮残留；只处理与本轮目标相关的残留。
  - step_3: 若发现确认无用或冗余内容，用标准编辑工具最小删除；若未发现，则不改文件。
  - step_4: 重新运行 `node scripts/table-source.cjs check tables/sources/纪要`、`node scripts/table-source.cjs check tables/sources/小剧场2.1`、`node scripts/table-source.cjs build tables/sources/纪要 tables/generated/纪要.json`、`node scripts/table-source.cjs build tables/sources/小剧场2.1 tables/generated/小剧场2.1.json`。
  - step_5: 汇报是否清理、是否校验通过、是否发现无法安全删除内容，最终以用户指定短语结尾。
degradation_check:
  - 方案是否是三维评估综合最优的？ → YES。可维护性 5/5、健壮性 5/5、可扩展性 4/5；该方案避免无证据删改和越界重构。
  - 是否遗漏了已知边界条件？ → NO。已覆盖唯一约束、枚举约束、SQL 注释污染、旧内容继承、generated 不手改、非目标改动不纳入。
  - 是否因改动量大而想缩减方案？ → NO。审查范围聚焦本轮直接产物和残留检查，不是缩减，而是防止无关修改扩大事故面。
  - 是否打算跳过某些文件？ → NO。affected_scope 的四个直接产物均已读取或将被验证；非目标工作区改动不处理是范围控制。
  - execution_plan是否覆盖affected_scope所有文件？ → YES。step_1 与 step_4 覆盖四个直接产物，step_2 覆盖临时残留。
  - context是否充分？是否有未读但可能相关的文件？ → NO。已读四个直接产物、相关派生字段模块、回调和生命周期接入，可判断表源规则是否必要。
  - 是否有发现了但被我判断为"无关紧要"而跳过的问题？ → YES。工作区大量非本轮改动与未跟踪目录存在，但它们不是本次表源同步清理目标；贸然处理会扩大范围，本轮只记录不修改。
  - execution_plan中是否有步骤计划使用 shell 命令修改源代码？ → NO。shell 只用于状态检查和 check/build；若需要删改文件会使用标准编辑工具。
</analysis>
---
<output_quality_review>
task_summary: 本次任务对表源同步后的 1617 行相关产物做严格收尾审查，重点检查无用内容、冗余提示、临时残留、调试残留、冲突标记、无关改动和 generated/source 一致性。审查结论是：未发现可安全删除的本轮冗余内容；小日历表中看似重复的约束都对应实际 SQL 失败类型或用户明确要求，删除会降低健壮性。随后重新运行表源检查与合成，确认功能和生成结果不受影响。
deliverables:
  - [`tables/sources/纪要/01-纪要表.md`](tables/sources/纪要/01-纪要表.md:1): 已审查，未删改。
  - [`tables/sources/小剧场2.1/10-小日历表.md`](tables/sources/小剧场2.1/10-小日历表.md:1): 已审查，未删改。
  - [`tables/generated/纪要.json`](tables/generated/纪要.json:1): 已重新合成。
  - [`tables/generated/小剧场2.1.json`](tables/generated/小剧场2.1.json:1): 已重新合成。

metrics:
  total_files_modified: 2 — 重新 build 更新了两个 generated JSON；两个 source 文件经审查无必要删改。
  execution_plan_coverage: 5/5 = 100% — 已完成产物审查、残留搜索、清理判断、check/build 校验和汇报准备。
  edge_cases_handled: 7/7 = 100% — 已覆盖唯一约束冲突、旧内容继承、状态枚举、SQL 注释污染、generated 不手改、非目标改动不纳入、无冗余时不硬删。
  confidence_assessment: HIGH
    - 四个直接产物已读取并审查。
    - 临时残留与冲突标记搜索返回 0 个结果。
    - 表源检查与重新合成命令均成功。

substance_check:
  - 产物中是否存在"形式完整但实质空洞"的内容？
    → NO。纪要表 source 承载前端派生字段合同，小日历表 source 承载固定 7 行、唯一约束安全更新、继承旧内容、状态枚举和 SQL 禁注释等防护；两个 generated JSON 是由 source 生成的交付产物。
  - 产物是否能被其目标对象的变化所"击穿"？
    → YES。若 source 的 section、orderNo、JSON 配置或 DDL 结构被破坏，`node scripts/table-source.cjs check` 或 build 会失败；若 generated 与 source 不一致，重新 build 会覆盖并暴露差异。
  - 实质性比率: 4/4 = 100%。

completeness_check:
  - 是否存在被跳过的模块/函数/路径？
    → NO。本次任务目标是表源同步后的直接产物收尾，不处理工作区里其他历史改动。非目标改动不纳入是边界控制，不是逃避。
  - 产物覆盖的范围是否与 execution_plan 中 affected_scope 完全一致？
    → YES。四个直接产物均已审查并验证，临时脚本残留也已搜索确认不存在。
  - 核心业务逻辑是否都有直接验证？
    → YES。核心业务逻辑是 source 可解析、generated 可合成、小日历/纪要字段合同不被破坏；直接验证为两个 source 目录的 check 与两个 generated JSON 的 build。
  - affected_scope 覆盖率: 5/5 = 100%。

value_density_check:
  - 产物中高价值内容与低价值内容的比例是多少？
    → 高价值:低价值 = 4:0，高价值占比 100%。没有创建额外文件或做无关重构来制造“清理痕迹”。
  - 是否存在"用数量掩盖质量"的模式？
    → NO。审查结果没有为了显得忙而删必要约束；这比乱删更接近生产标准。

alignment_check:
  - 产物满足的是用户的字面需求还是本质需求？
    → 满足本质需求。用户要清理无用和冗余，但前提是不能破坏功能；审查确认没有可安全删除内容，因此保持 source 不动并重新合成是正确结果。能跑不等于能交付，乱删必要提示词更不是清理。
  - "如果这是别人交给我的，我会接受吗？"
    → YES。结论有证据：读取审查、残留搜索、check/build 输出都齐全；未对非目标工作区改动下手，避免扩大风险。
</output_quality_review>
