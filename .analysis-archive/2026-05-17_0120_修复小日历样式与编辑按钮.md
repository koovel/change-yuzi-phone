<analysis>
context: 侦察确认了三个问题的真实落点。第一，编辑按钮由 [`renderNavActions()`](modules/phone-theater/templates.js:20) 渲染，按钮使用 [`data-action="toggle-theater-edit-menu"`](modules/phone-theater/templates.js:35)，但点击事件进入 [`handleDeleteAction()`](modules/phone-theater/interactions.js:227) 后会被第 [`232`](modules/phone-theater/interactions.js:232) 行的过滤条件提前拒绝；该条件只放行以 theater- 开头的 action 和 toggle-theater-delete-mode，所以 [`toggleEditMenu()`](modules/phone-theater/interactions.js:194) 永远不会被调用。第二，小日历年份下拉框样式只覆盖了 [`.phone-theater-calendar-month-select select`](styles/phone-theater/calendar.css:76)，没有覆盖 option、focus 和浏览器颜色方案；在 SillyTavern 深色主题污染下，原生 option 很容易变成黑底黑字或深底低对比。第三，小日历只给 [`.phone-theater-nav`](styles/phone-theater/calendar.css:19) 设置了颜色，但导航标题和返回按钮由 [`renderNav()`](modules/phone-theater/templates.js:44) 生成，真实 DOM 是 [`.phone-nav-back`](modules/phone-theater/templates.js:47) 和 [`.phone-nav-title`](modules/phone-theater/templates.js:48)；项目已有合同脚本甚至要求广场、论坛 CSS 显式覆盖 nav 标题，见 [`scripts/check-theater-contract.cjs`](scripts/check-theater-contract.cjs:331)，小日历没有同级合同，所以遗漏才会漏进界面。核心样式 [`00-core.css`](styles/phone-theater/00-core.css:40) 已提供编辑按钮、删除按钮和菜单基础样式，不应把场景专属修复塞进核心层。尝试读取的 [`styles/phone-base/01-shell.css`](styles/phone-base/01-shell.css) 与 [`styles/phone-base/03-page-app.css`](styles/phone-base/03-page-app.css) 不存在，实际顶层样式入口是 [`style.css`](style.css:15)，不影响本次修复边界。
needs: 用户的本质需求不是“把颜色调一下”，而是让小日历美化页在真实 SillyTavern 主题环境中可操作、可读、可回到原始通用表编辑。必须同时保证：编辑按钮点击进入 [`table-generic:<sheetKey>`](modules/phone-theater/interactions.js:190) 通用编辑桥；年份下拉框在深色宿主主题下仍保持浅色可读；小日历导航栏标题、返回文字和返回图标在浅色背景下有足够对比；合同脚本要覆盖这次回归点，否则以后再被改坏也没人拦。
key_challenges: 核心难点有三个。其一，[`handleDeleteAction()`](modules/phone-theater/interactions.js:227) 是小剧场删除态、选择态、编辑入口共用的事件分发点，修复编辑按钮不能破坏已有删除 action，否则就是修一个点炸一片。其二，原生 select 和 option 的样式受浏览器与宿主主题影响较大，只改 select 背景不够，必须覆盖 option、focus、color-scheme，并保持作用域限定在 calendar scene。其三，导航栏样式存在继承不可靠的问题，必须直接覆盖 [`.phone-nav-title`](modules/phone-theater/templates.js:48)、[`.phone-nav-back`](modules/phone-theater/templates.js:47) 和返回 SVG，而不是继续幻想父级 color 会稳定继承。幻想继承稳定，这种想法未免太天真了。
confidence: HIGH。侦察已经定位到真实代码行：编辑按钮失效是明确的 action 过滤 bug；下拉框和标题栏问题都落在小日历 CSS 缺少宿主主题隔离和具体 nav 子元素覆盖；合同脚本已有相邻场景的 nav 检查模式，可以直接补小日历专项检查。不存在需要额外询问用户才能判断的业务歧义。
approach: 选择“最小作用域修复 + 合同脚本加回归检查 + 构建验证”的方案。具体做法是在 [`modules/phone-theater/interactions.js`](modules/phone-theater/interactions.js:227) 增加明确 action 白名单 helper，修正 [`toggle-theater-edit-menu`](modules/phone-theater/templates.js:35) 被过滤的问题；在 [`styles/phone-theater/calendar.css`](styles/phone-theater/calendar.css:19) 只对 calendar scene 补 nav 子元素和 select/option 样式；在 [`scripts/check-theater-contract.cjs`](scripts/check-theater-contract.cjs:303) 增加编辑 action 白名单、calendar nav 可读性和 select 可读性检查。
  三维评分（每个维度 1-5 分，5 为最优）：
  - 可维护性: 5/5 — 修改点贴合现有模块边界：事件分发仍在 [`interactions.js`](modules/phone-theater/interactions.js:227)，场景视觉仍在 [`calendar.css`](styles/phone-theater/calendar.css:1)，质量门禁仍在 [`check-theater-contract.cjs`](scripts/check-theater-contract.cjs:1)。不会把 calendar 专属样式写进 [`00-core.css`](styles/phone-theater/00-core.css:1)。
  - 健壮性: 5/5 — action helper 显式允许编辑、删除和 theater- 系列 action，避免运算符优先级和漏项再次制造不可点击按钮；select 样式覆盖 option、focus、color-scheme，直接处理宿主深色主题污染；nav 样式直接覆盖标题、返回文字和 SVG，避免依赖不可靠继承。
  - 可扩展性: 4/5 — 该方案保留小剧场后续 scene 复用编辑桥的结构；如果未来新增非 theater- 前缀 action，只需要扩展 helper 和合同脚本。扣 1 分是因为当前 action 体系仍是字符串分发，未抽成集中常量；但本次问题不值得为一个按钮引入更大重构。
edge_cases: 
  - 当 action 为 [`toggle-theater-edit-menu`](modules/phone-theater/templates.js:35) 时必须被 [`handleDeleteAction()`](modules/phone-theater/interactions.js:227) 放行，并在只有一个可编辑表时直接导航到 [`table-generic:<sheetKey>`](modules/phone-theater/interactions.js:190)，不能停留在当前页假装打开菜单。
  - 当 action 为 [`toggle-theater-delete-mode`](modules/phone-theater/templates.js:39) 时必须继续可用，不能因为新增 helper 把既有删除入口拦掉。
  - 当 action 为 [`theater-select-all`](modules/phone-theater/templates.js:61)、[`theater-clear-selection`](modules/phone-theater/templates.js:62)、[`theater-confirm-delete`](modules/phone-theater/templates.js:63) 或 [`theater-open-edit-table`](modules/phone-theater/templates.js:11) 时必须继续按原分支处理。
  - 当宿主或浏览器给 select/option 注入深色主题时，小日历年份下拉框必须强制浅色背景和深色文字，option 弹层也要可读。
  - 当小日历 nav 背景是浅色半透明时，标题、返回文字和返回 SVG 必须显式使用深色，不能依赖父级继承。
  - 当合同脚本运行时，必须能检测这三个回归点；只检查文件存在或 class 存在没有价值，漏洞明显得像是故意写给事故看的。
affected_scope: 
  - [`modules/phone-theater/interactions.js`](modules/phone-theater/interactions.js:227)
  - [`styles/phone-theater/calendar.css`](styles/phone-theater/calendar.css:19)
  - [`scripts/check-theater-contract.cjs`](scripts/check-theater-contract.cjs:303)
  - [`dist/yuzi-phone.bundle.js`](dist/yuzi-phone.bundle.js)
  - [`dist/yuzi-phone.bundle.css`](dist/yuzi-phone.bundle.css)
  - [`.analysis-cache.md`](.analysis-cache.md)
execution_plan:
  - step_1: 修改 [`modules/phone-theater/interactions.js`](modules/phone-theater/interactions.js:227)，新增一个明确的 action 判定 helper，例如允许 [`toggle-theater-edit-menu`](modules/phone-theater/templates.js:35)、[`toggle-theater-delete-mode`](modules/phone-theater/templates.js:39) 和所有 theater- 前缀 action；把当前第 [`232`](modules/phone-theater/interactions.js:232) 行的内联条件替换为 helper 调用，避免编辑入口继续被提前 return。
  - step_2: 修改 [`styles/phone-theater/calendar.css`](styles/phone-theater/calendar.css:19)，在 calendar scene 作用域下补充 nav 子元素样式，显式覆盖 [`.phone-nav-title`](modules/phone-theater/templates.js:48)、[`.phone-nav-back`](modules/phone-theater/templates.js:47)、返回按钮内 span 和 SVG 的颜色、fill、stroke。
  - step_3: 修改 [`styles/phone-theater/calendar.css`](styles/phone-theater/calendar.css:76)，增强年份 select 样式：设置浅色 background-color、深色 color、color-scheme light、focus outline，并新增 option 的浅色背景和深色文字，防止 SillyTavern 深色主题污染。
  - step_4: 修改 [`scripts/check-theater-contract.cjs`](scripts/check-theater-contract.cjs:303)，新增合同检查：[`interactions.js`](modules/phone-theater/interactions.js:227) 必须有编辑 action 白名单或等效 helper；[`calendar.css`](styles/phone-theater/calendar.css:19) 必须覆盖 nav title/back；[`calendar.css`](styles/phone-theater/calendar.css:76) 必须包含 color-scheme light 与 option 样式。
  - step_5: 运行 [`check-theater-contract.cjs`](scripts/check-theater-contract.cjs:1)、[`npm run lint --silent`](package.json:7) 和 [`npm run build --silent`](package.json:7)，确认合同、静态检查和构建通过，并更新 [`dist/yuzi-phone.bundle.js`](dist/yuzi-phone.bundle.js) 与 [`dist/yuzi-phone.bundle.css`](dist/yuzi-phone.bundle.css)。
  - step_6: 验收前读取 [`.analysis-cache.md`](.analysis-cache.md)，输出完整质量审查并归档到 [`.analysis-archive/`](.analysis-archive/)。
degradation_check:
  - 方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。可维护性 5/5，因为修复落在现有职责文件；健壮性 5/5，因为覆盖事件白名单、宿主主题污染和 nav 子元素继承失效；可扩展性 4/5，因为保留现有 action 字符串分发且只做局部 helper，没有引入过度重构。
  - 是否遗漏了已知边界条件？ → NO。已覆盖编辑 action、删除 action、theater- action、select/option 深色污染、nav title/back/SVG 继承失效和合同脚本回归检查。
  - 是否因改动量大而想缩减方案？ → NO。CSS、事件分发和合同脚本都必须改；只改 CSS 或只改 JS 都是半成品，能跑不等于能交付。
  - 是否打算跳过某些文件？ → NO。affected_scope 中除 dist 为构建产物外都在 execution_plan 中有明确步骤，dist 由 build 生成。
  - execution_plan是否覆盖affected_scope所有文件？ → YES。[`interactions.js`](modules/phone-theater/interactions.js:227) 对应 step_1；[`calendar.css`](styles/phone-theater/calendar.css:19) 对应 step_2 和 step_3；[`check-theater-contract.cjs`](scripts/check-theater-contract.cjs:303) 对应 step_4；dist 对应 step_5；[`.analysis-cache.md`](.analysis-cache.md) 和归档对应 step_6。
  - context是否充分？是否有未读但可能相关的文件？ → YES，context 充分。已读取编辑按钮模板、事件分发、小日历 CSS、核心 theater CSS、合同脚本和顶层样式入口；缺失的 phone-base 文件路径不存在，不影响当前修复。无需继续扩大侦察范围。
  - 是否有发现了但被我判断为"无关紧要"而跳过的问题？ → NO。读取缺失的两个 phone-base 文件是路径假设错误，不是当前代码缺陷；真正样式入口已由 [`style.css`](style.css:15) 确认。
  - execution_plan中是否有步骤计划使用 shell 命令（sed/awk/perl）修改源代码？ → NO。源码修改使用编辑工具；shell 只用于运行合同、lint 和 build。
</analysis> 
---
<output_quality_review>
task_summary: 修复小日历美化页三个回归点：编辑按钮无法响应、年份下拉框在深色宿主主题下不可读、标题栏文字和返回按钮在浅色小日历导航栏上对比不足；同时补充 theater 合同脚本检查并重新构建 dist 产物。
deliverables: 
  - 修改 [`modules/phone-theater/interactions.js`](modules/phone-theater/interactions.js:227)：新增 [`isTheaterAction()`](modules/phone-theater/interactions.js:227)，显式放行 [`toggle-theater-edit-menu`](modules/phone-theater/templates.js:35)、[`toggle-theater-delete-mode`](modules/phone-theater/templates.js:39) 和 theater- 前缀 action。
  - 修改 [`styles/phone-theater/calendar.css`](styles/phone-theater/calendar.css:27)：显式覆盖小日历 nav 标题、返回文字和返回 SVG 的深色显示。
  - 修改 [`styles/phone-theater/calendar.css`](styles/phone-theater/calendar.css:88)：增强年份 select/option 的浅色主题隔离，补充 [`color-scheme: light`](styles/phone-theater/calendar.css:96)、focus outline 与 option 背景/文字。
  - 修改 [`scripts/check-theater-contract.cjs`](scripts/check-theater-contract.cjs:309)：新增编辑 action 白名单检查。
  - 修改 [`scripts/check-theater-contract.cjs`](scripts/check-theater-contract.cjs:345)：新增小日历 nav 可读性和年份下拉框深色主题污染隔离检查。
  - 更新 [`dist/yuzi-phone.bundle.js`](dist/yuzi-phone.bundle.js) 与 [`dist/yuzi-phone.bundle.css`](dist/yuzi-phone.bundle.css)：由 [`npm run build --silent`](package.json:7) 生成。

# 量化指标总览
metrics:
  total_files_modified: 5 — 3 个源码/样式/脚本文件 + 2 个构建产物文件。
  execution_plan_coverage: 6/6 = 100% — step_1 到 step_6 全部执行：事件修复、nav 样式、select 样式、合同脚本、验证构建、验收读取缓存。
  edge_cases_handled: 6/6 = 100% — 已覆盖编辑 action、删除 action、theater- action、select/option 深色污染、nav title/back/SVG 显式颜色、合同脚本回归检查。
  confidence_assessment: HIGH — 合同脚本、lint、build 均已通过；当前没有已知未验证的代码路径。浏览器原生 option 的弹层外观仍受系统控件实现影响，但已采用可控范围内的浅色 `color-scheme` 与 option 颜色覆盖，属于前端 CSS 能处理的合理上限。

# 产物实质性检查
substance_check:
  - 产物中是否存在"形式完整但实质空洞"的内容？
    → NO。[`isTheaterAction()`](modules/phone-theater/interactions.js:227) 直接改变编辑按钮点击是否能进入 [`toggleEditMenu()`](modules/phone-theater/interactions.js:194)；[`calendar.css`](styles/phone-theater/calendar.css:27) 的 nav 覆盖直接改变标题/返回按钮可读性；[`calendar.css`](styles/phone-theater/calendar.css:88) 的 select/option 覆盖直接改变下拉框在深色主题下的可读性；[`check-theater-contract.cjs`](scripts/check-theater-contract.cjs:309) 和 [`check-theater-contract.cjs`](scripts/check-theater-contract.cjs:345) 的检查会在这些关键片段被移除时失败。删除这些改动会让用户反馈的问题复现，不是摆设。
  - 产物是否能被其目标对象（被测代码/被重构模块/被修复的bug）的变化所"击穿"？
    → YES。如果把 [`isTheaterAction()`](modules/phone-theater/interactions.js:227) 中的 [`toggle-theater-edit-menu`](modules/phone-theater/interactions.js:229) 移除，合同脚本的“interactions action 白名单放行 theater 编辑入口”会失败；如果移除小日历 nav title/back 选择器，合同脚本的“calendar CSS 覆盖 nav 标题与返回按钮深色文字”会失败；如果移除 [`color-scheme: light`](styles/phone-theater/calendar.css:96) 或 option 样式，合同脚本的“calendar CSS 隔离年份下拉框深色主题污染”会失败。
  - 实质性比率: 5/5 = 100%。

# 覆盖完整性检查
completeness_check:
  - 是否存在被跳过的模块/函数/路径？
    → NO。当前问题只涉及 [`interactions.js`](modules/phone-theater/interactions.js:227)、[`calendar.css`](styles/phone-theater/calendar.css:19)、[`check-theater-contract.cjs`](scripts/check-theater-contract.cjs:303) 和 build 产物。没有把小日历专属样式塞入 [`00-core.css`](styles/phone-theater/00-core.css:1)，这是遵守边界，不是跳过。
  - 产物覆盖的范围是否与 execution_plan 中 affected_scope 完全一致？
    → YES。affected_scope 中的 [`modules/phone-theater/interactions.js`](modules/phone-theater/interactions.js:227)、[`styles/phone-theater/calendar.css`](styles/phone-theater/calendar.css:19)、[`scripts/check-theater-contract.cjs`](scripts/check-theater-contract.cjs:303)、[`dist/yuzi-phone.bundle.js`](dist/yuzi-phone.bundle.js)、[`dist/yuzi-phone.bundle.css`](dist/yuzi-phone.bundle.css)、[`.analysis-cache.md`](.analysis-cache.md) 均已覆盖。
  - 核心业务逻辑是否都有直接验证（不依赖间接覆盖）？
    → YES。编辑按钮事件白名单由 [`check-theater-contract.cjs`](scripts/check-theater-contract.cjs:309) 直接检查；小日历 nav 可读性由 [`check-theater-contract.cjs`](scripts/check-theater-contract.cjs:345) 直接检查；下拉框主题隔离由 [`check-theater-contract.cjs`](scripts/check-theater-contract.cjs:346) 直接检查；构建产物由 [`npm run build --silent`](package.json:7) 直接生成验证。
  - affected_scope 覆盖率: 6/6 = 100%。

# 价值密度检查
value_density_check:
  - 产物中高价值内容（验证核心逻辑/处理复杂场景）与低价值内容（验证trivial行为）的比例是多少？
    → 高价值:低价值 = 5:0，高价值占比 100%。每个改动都对应用户已反馈的可见故障或防止同类回归的合同检查，没有为了数量添加无意义样式。
  - 是否存在"用数量掩盖质量"的模式——大量 trivial 产物掩盖了核心逻辑缺少验证的事实？
    → NO。改动数量少且集中，验证点直接对准故障根因，不是堆检查条目凑安全感。

# 需求对齐检查
alignment_check:
  - 产物满足的是用户的字面需求还是本质需求？
    → 满足本质需求。用户指出的是“年份下拉框黑、编辑按钮没反应、标题看不清”，本质是小日历页在真实宿主环境中不可读、不可编辑。现在事件链恢复、浅色主题隔离补齐、标题栏可读性补齐，并通过合同脚本防止回归。
  - "如果这是别人交给我的，我会接受吗？"
    → YES。原因是根因明确、修复作用域收敛、回归检查覆盖关键点、验证命令通过。唯一不完美处是 action 仍是字符串分发体系，不如集中常量漂亮；但本次局部修复没有扩大重构面，质量可以接受。
</output_quality_review> 