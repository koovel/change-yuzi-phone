<analysis>
context: 侦察确认本轮不是继续“调一下下拉框颜色”，而是要替换错误交互模型。小日历当前在 [`renderContent()`](modules/phone-theater/scenes/calendar.js:276) 内使用原生年份下拉元素，年份候选由 [`Array.from()`](modules/phone-theater/scenes/calendar.js:289) 写死为当前年前后 4 年，总共 9 项；用户截图证明弹层仍被 SillyTavern 深色主题污染，普通 CSS 对原生弹层控制不稳定。当前年份变更逻辑由 [`handleChange()`](modules/phone-theater/scenes/calendar.js:364) 监听原生变更，再调用 [`rerenderMonth()`](modules/phone-theater/scenes/calendar.js:305)。样式层旧规则集中在 [`calendar.css`](styles/phone-theater/calendar.css:88) 到 [`calendar.css`](styles/phone-theater/calendar.css:108)，这些规则只能影响页面内控件，不能可靠控制浏览器绘制的弹层。编辑桥方面，当前 [`navigateToEditableTable()`](modules/phone-theater/interactions.js:187) 通过 [`navigateTo()`](modules/phone-core/routing.js:52) 进入 `table-generic:<sheetKey>`，[`navigateBack()`](modules/phone-core/routing.js:67) 会回到来源小剧场美化页；[`loadRouteRenderer()`](modules/phone-core/route-renderer.js:64) 已支持 `table-generic` 强制通用表列表。架构文档 [`docs/architecture-guide.md`](docs/architecture-guide.md:110) 的路由段落和 [`docs/architecture-guide.md`](docs/architecture-guide.md:274) 的 Table Viewer 分流段落尚未记录编辑桥返回语义；扩展规范 [`theater-scene-extension-spec.md`](docs/reference/theater-scene-extension-spec.md:77) 已有 `editableTables` 字段说明，但没有写清返回语义和 scene 不自行实现原始编辑。合同脚本 [`check-theater-contract.cjs`](scripts/check-theater-contract.cjs:346) 仍在检查旧的原生下拉隔离片段，实际已经被截图击穿。
needs: 本轮本质目标有三个。第一，正式把“从小剧场美化页进入原始通用表编辑，返回时回到来源美化页”的行为写入架构和扩展规范，避免它继续作为隐性副作用存在。第二，把小日历年份选择从原生下拉迁移为小手机内部自定义年份 picker，彻底摆脱 SillyTavern 深色主题和浏览器原生弹层。第三，更新合同脚本，不再检查旧的原生下拉补丁，而是禁止小日历继续使用原生年份下拉，并检查自定义 picker 的关键结构和样式。
key_challenges: 核心难点有四个。其一，必须替换交互模型而不是扩大年份数组；只把 9 年改成 101 年仍然会被黑色原生弹层击穿，那是补丁，不是修复。其二，自定义 picker 要和现有 [`rerenderMonth()`](modules/phone-theater/scenes/calendar.js:305) 共用状态，避免产生“按钮显示年份”和月历真实年份不同步。其三，picker 展开态要存在于小手机 DOM 内部，必须能关闭、滚动、选择，并且不能影响日期格点击。其四，文档与合同脚本要同步，不然这次修复会成为另一个无记录约定。
confidence: HIGH。已确认原生年份下拉的渲染位置、事件位置、样式位置、路由返回语义和合同脚本旧检查位置；方案不依赖未验证的外部 API，也不需要用户补充交互细节。
approach: 推荐实施“自定义年份 picker + 文档化编辑桥返回语义 + 合同脚本改为禁止原生年份下拉”的方案。具体做法是在 [`calendar.js`](modules/phone-theater/scenes/calendar.js:276) 中新增年份范围构造、picker 渲染、展开关闭和年份选择逻辑；在 [`calendar.css`](styles/phone-theater/calendar.css:74) 中删除原生年份下拉规则，新增内部弹层、滚动列表、选中态和焦点态样式；在 [`docs/architecture-guide.md`](docs/architecture-guide.md:110) 与 [`theater-scene-extension-spec.md`](docs/reference/theater-scene-extension-spec.md:77) 中记录编辑桥返回语义；在 [`check-theater-contract.cjs`](scripts/check-theater-contract.cjs:346) 中替换旧检查，强制小日历 scene 不再渲染原生年份下拉。
  三维评分（每个维度 1-5 分，5 为最优）：
  - 可维护性: 5/5 — 修改落在已有职责文件：小日历逻辑在 [`calendar.js`](modules/phone-theater/scenes/calendar.js:276)，小日历视觉在 [`calendar.css`](styles/phone-theater/calendar.css:74)，架构约定在文档，回归检查在 [`check-theater-contract.cjs`](scripts/check-theater-contract.cjs:1)。没有把 scene 专属 picker 塞进核心 shell。
  - 健壮性: 5/5 — 自定义 picker 不使用浏览器原生弹层，避免宿主深色主题污染；年份范围由代码生成并可滚动；选择后复用 [`rerenderMonth()`](modules/phone-theater/scenes/calendar.js:305)，状态来源统一。
  - 可扩展性: 4/5 — 后续可扩展成月份/年份组合 picker 或年份输入跳转；扣 1 分是因为当前小日历仍采用单文件 scene 内部函数组织，尚未拆出独立 picker 模块，但本次范围内拆模块收益不高。
edge_cases:
  - 小日历 picker 展开后点击年份按钮，应更新当前展示年份、关闭 picker、刷新月历网格，并保持当前已选日期键不被无意义清空。
  - 小日历 picker 展开后点击展开按钮，应能收起；点击上一月、下一月、日期格时，应先关闭 picker，避免浮层停留在错误月份上。
  - 年份候选范围不能再只有 9 项，应覆盖足够宽的范围，例如当前年前后 50 年，并支持内部滚动。
  - 自定义 picker 不能使用原生年份下拉元素，否则 SillyTavern 深色主题仍可能污染弹层。
  - 文档必须明确编辑桥返回语义：通过 [`navigateTo()`](modules/phone-core/routing.js:52) 进入通用编辑页，返回由 [`navigateBack()`](modules/phone-core/routing.js:67) 回到来源小剧场美化页。
  - 合同脚本必须能在小日历重新引入原生年份下拉、缺少 picker class、缺少滚动样式、缺少文档说明时失败。
affected_scope:
  - [`docs/architecture-guide.md`](docs/architecture-guide.md:110)
  - [`docs/reference/theater-scene-extension-spec.md`](docs/reference/theater-scene-extension-spec.md:77)
  - [`modules/phone-theater/scenes/calendar.js`](modules/phone-theater/scenes/calendar.js:276)
  - [`styles/phone-theater/calendar.css`](styles/phone-theater/calendar.css:74)
  - [`scripts/check-theater-contract.cjs`](scripts/check-theater-contract.cjs:346)
  - [`dist/yuzi-phone.bundle.js`](dist/yuzi-phone.bundle.js)
  - [`dist/yuzi-phone.bundle.css`](dist/yuzi-phone.bundle.css)
  - [`.analysis-cache.md`](.analysis-cache.md)
execution_plan:
  - step_1: 更新 [`docs/architecture-guide.md`](docs/architecture-guide.md:110)，在路由系统或 Table Viewer 入口分流段落补充 `table-generic:<sheetKey>` 编辑桥语义，明确从小剧场美化页进入编辑桥后，返回应回到来源美化页。
  - step_2: 更新 [`docs/reference/theater-scene-extension-spec.md`](docs/reference/theater-scene-extension-spec.md:77)，补充 `editableTables` 交互契约：scene 只声明可编辑原始表，由 shell 进入通用编辑桥；单表直接跳转，多表菜单选择；返回沿 route history 回来源美化页。
  - step_3: 修改 [`modules/phone-theater/scenes/calendar.js`](modules/phone-theater/scenes/calendar.js:276)，移除原生年份下拉渲染，新增年份范围构造函数、自定义年份 picker 渲染函数和展开态 DOM 标记。
  - step_4: 修改 [`modules/phone-theater/scenes/calendar.js`](modules/phone-theater/scenes/calendar.js:333)，把原 [`handleChange()`](modules/phone-theater/scenes/calendar.js:364) 替换为 click 分支：处理展开/收起 picker、选择年份、月切换时关闭 picker、日期点击时关闭 picker。
  - step_5: 修改 [`styles/phone-theater/calendar.css`](styles/phone-theater/calendar.css:74)，删除旧原生下拉样式，新增自定义年份按钮、浮层、滚动列表、选中年份、hover/focus 和深色主题隔离样式。
  - step_6: 修改 [`scripts/check-theater-contract.cjs`](scripts/check-theater-contract.cjs:346)，将“原生下拉隔离”检查替换为“禁止原生年份下拉 + 自定义年份 picker class 齐全 + picker 滚动样式齐全”；同时补充架构文档和扩展规范的编辑桥返回语义检查。
  - step_7: 切换到 Code 模式执行非 Markdown 文件修改；运行合同检查、lint、build，并更新 dist 产物。
  - step_8: 验收前读取 [`.analysis-cache.md`](.analysis-cache.md)，输出质量审查并归档到 [`.analysis-archive/`](.analysis-archive/)。
degradation_check:
  - 方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。可维护性 5/5，修改落在现有职责边界；健壮性 5/5，彻底移除原生弹层依赖；可扩展性 4/5，未来可扩展 picker，但当前不做过度拆分。
  - 是否遗漏了已知边界条件？ → NO。已覆盖年份范围、展开关闭、月切换关闭、日期点击关闭、返回语义文档化、合同脚本禁止回归。
  - 是否因改动量大而想缩减方案？ → NO。明确拒绝“扩大原生下拉年份范围”的低质量补丁。
  - 是否打算跳过某些文件？ → NO。文档、scene、CSS、合同脚本、dist、分析缓存均在 execution_plan 中覆盖。
  - execution_plan是否覆盖affected_scope所有文件？ → YES。两个文档对应 step_1 和 step_2；scene 对应 step_3 和 step_4；CSS 对应 step_5；合同脚本对应 step_6；dist 对应 step_7；分析缓存和归档对应 step_8。
  - context是否充分？是否有未读但可能相关的文件？ → YES，context 充分。已读取路由系统、Table Viewer 分流、小剧场规范、calendar scene、calendar CSS、合同脚本相关段落。
  - 是否有发现了但被我判断为"无关紧要"而跳过的问题？ → NO。上一轮合同脚本只查片段存在的问题已纳入本轮修复范围。
  - execution_plan中是否有步骤计划使用 shell 命令（sed/awk/perl）修改源代码？ → NO。源码修改使用编辑工具；shell 只用于验证命令。
</analysis> 
---
<decision_point>
issue: 更新 [`docs/reference/theater-scene-extension-spec.md`](docs/reference/theater-scene-extension-spec.md:77) 时，编辑工具未找到预期 old_string。原因是我使用了带完整表格尾部的匹配块，但文件当前内容与我构造的片段在空行、字段顺序或不可见字符上存在差异。这个问题在侦察阶段没有预见到，因为我读取了目标行但没有在修改前重新确认包含 `editableTables` 行之后的精确上下文。
impact: YES。影响当前 execution_plan 的 step_2；如果不处理，就会导致扩展规范缺少编辑桥返回语义，合同脚本后续也不能可靠检查文档说明。
context_update: analysis 中 `confidence: HIGH` 仍成立，但 `execution_plan step_2` 的执行方式需要从“直接替换已知片段”改为“重新读取目标文件精确上下文后做窄范围插入”。affected_scope 不变。
options:
  - option_a:
      description: 重新读取 [`docs/reference/theater-scene-extension-spec.md`](docs/reference/theater-scene-extension-spec.md) 的 59-85 行，获取当前真实表格尾部和下一节标题，再用精确上下文在 `editableTables` 行后插入 `2.1 editableTables 编辑桥契约`。
      approach_evaluation: 可维护性 5/5，保持文档结构不变，只在字段说明后补充契约段落；健壮性 5/5，基于最新文件内容精确替换，避免误改；可扩展性 5/5，后续新增 scene 时能直接按该段落执行。
      edge_cases: 如果 `editableTables` 行实际不存在，需要改为在字段表格末尾插入并同步合同脚本检查；如果文档已有类似段落，需要避免重复写入。
      affected_scope_delta: 不新增文件，只改变 [`docs/reference/theater-scene-extension-spec.md`](docs/reference/theater-scene-extension-spec.md) 的修改方式。
  - option_b:
      description: 使用 `apply_diff` 按行号直接插入段落。
      approach_evaluation: 可维护性 4/5，能精确插入但依赖行号稳定；健壮性 3/5，若文件已被其他工具改动，行号可能漂移；可扩展性 4/5，结果可维护但执行方式比精确内容替换脆。
      edge_cases: 行号漂移会导致插入位置错误；当前工具要求 SEARCH 块精确匹配，仍需要读取真实上下文。
      affected_scope_delta: 不新增文件，但工具路径从 edit_file 改为 apply_diff。
  - option_c:
      description: 跳过扩展规范文档，只在架构文档中记录编辑桥语义，并在合同脚本不检查 specDoc。
      approach_evaluation: 可维护性 2/5，规范与架构文档会出现信息割裂；健壮性 1/5，未来新增 scene 仍可能不知道 `editableTables` 返回语义；可扩展性 1/5，直接破坏小剧场扩展文档作为接入契约的价值。
      edge_cases: 后续开发者只读扩展规范时会遗漏编辑桥行为，容易在 scene 内硬编码编辑器或返回目标。
      affected_scope_delta: 缩小 affected_scope，但这是质量下降。
recommendation: 选择 option_a。三维评估显示 option_a 在可维护性、健壮性、可扩展性均为 5/5；option_b 依赖行号，option_c 是明显降级。
execution_plan_update: step_2 更新为：先读取 [`docs/reference/theater-scene-extension-spec.md`](docs/reference/theater-scene-extension-spec.md) 中包含 `editableTables` 行和 `## 3. 数据读取与 ViewModel` 标题的精确上下文，再用 `edit_file` 在该位置插入 `### 2.1 editableTables 编辑桥契约` 段落。其他步骤不变。
deviation_audit:
  original_plan_excerpt: `- step_2: 更新 [`docs/reference/theater-scene-extension-spec.md`](docs/reference/theater-scene-extension-spec.md:77)，补充 `editableTables` 交互契约：scene 只声明可编辑原始表，由 shell 进入通用编辑桥；单表直接跳转，多表菜单选择；返回沿 route history 回来源美化页。`
  current_proposal: 先读取目标文件当前真实上下文，再在 `editableTables` 字段说明后插入同样的交互契约段落。
  diff_summary: 新增了重新读取精确上下文的前置动作；没有删减文档内容；没有缩小 affected_scope；没有改变最终交付目标。
  deviation_motive_check:
    - 措辞替换规则逐类检查：未命中降级措辞。
    - 偏离后的方案在三维评分上是否低于原方案？→ NO。原方案目标保持不变，执行方式更稳。
    - 偏离是否导致 affected_scope 缩小？→ NO。仍修改同一规范文档。
  self_dissection: 未触发。
degradation_check:
  - 推荐方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。option_a 三维评分 5/5、5/5、5/5，优于依赖行号的 option_b 和跳过规范的 option_c。
  - 推荐方案是否遗漏了新发现的边界条件？ → NO。已考虑目标行不存在、已有类似段落、上下文漂移。
  - 是否因为想尽快完成而选择了改动量小的方案？ → NO。没有选择跳过文档的 option_c，而是补充读取后继续完成原目标。
  - 修改后的execution_plan是否仍覆盖所有affected_scope？ → YES。affected_scope 不变。
  - 是否有发现了但被判断为"无关紧要"而跳过的问题？ → NO。工具失败已进入 decision_point 并修正执行方式。
  - options 是否包含至少三个方案？ → YES。包含 option_a、option_b、option_c。
  - 是否因为工具报错而准备换用 shell 命令修改源代码？ → NO。继续使用 read_file 和 edit_file，不用 shell 修改源代码。
  - deviation_audit 是否触发了 self_dissection？ → NO。未触发偷懒判定。
</decision_point> 
---
<output_quality_review>
task_summary: 本次任务将小日历年份选择从不可控的浏览器原生下拉替换为小手机内部自定义年份 picker，并把“从小剧场美化页进入原始通用表编辑后返回来源美化页”的行为正式写入架构合同和扩展规范。助手，你之前那个原生下拉继续修 CSS 的方向确实太天真了；宿主深色主题污染的是浏览器绘制弹层，不替换交互模型就是继续给事故补妆。
deliverables:
  - 修改 [`docs/architecture-guide.md`](docs/architecture-guide.md:120)：新增 `table-generic:<sheetKey>` route 与小剧场编辑桥返回语义说明。
  - 修改 [`docs/reference/theater-scene-extension-spec.md`](docs/reference/theater-scene-extension-spec.md:79)：新增 `editableTables` 编辑桥契约，明确不得在 scene 内手搓编辑跳转。
  - 修改 [`modules/phone-theater/scenes/calendar.js`](modules/phone-theater/scenes/calendar.js:277)：移除原生年份下拉，新增年份范围构造、自定义年份 picker、展开关闭和选择年份交互。
  - 修改 [`styles/phone-theater/calendar.css`](styles/phone-theater/calendar.css:89)：新增自定义年份 picker 样式、滚动面板和选中态，移除原生 select 依赖。
  - 修改 [`scripts/check-theater-contract.cjs`](scripts/check-theater-contract.cjs:37)：新增架构文档合同检查，禁止小日历回退到原生年份 select，并检查 picker 关键结构和滚动样式。
  - 更新 [`dist/yuzi-phone.bundle.js`](dist/yuzi-phone.bundle.js) 与 [`dist/yuzi-phone.bundle.css`](dist/yuzi-phone.bundle.css)：通过 [`npm run build --silent`](package.json:7) 生成最新打包产物。
  - 维护 [`.analysis-cache.md`](.analysis-cache.md:1)：记录 analysis、两个 decision_point 和本验收报告。

# 量化指标总览
metrics:
  total_files_modified: 8 — [`docs/architecture-guide.md`](docs/architecture-guide.md:120)、[`docs/reference/theater-scene-extension-spec.md`](docs/reference/theater-scene-extension-spec.md:79)、[`modules/phone-theater/scenes/calendar.js`](modules/phone-theater/scenes/calendar.js:277)、[`styles/phone-theater/calendar.css`](styles/phone-theater/calendar.css:89)、[`scripts/check-theater-contract.cjs`](scripts/check-theater-contract.cjs:37)、[`dist/yuzi-phone.bundle.js`](dist/yuzi-phone.bundle.js)、[`dist/yuzi-phone.bundle.css`](dist/yuzi-phone.bundle.css)、[`.analysis-cache.md`](.analysis-cache.md:1)。
  execution_plan_coverage: 8/8 = 100% — step_1 到 step_8 均已完成：文档、规范、scene、CSS、合同脚本、验证构建和验收前缓存读取均已覆盖。
  edge_cases_handled: 6/6 = 100% — 自定义 picker 年份选择关闭并刷新、展开按钮收起、月切换关闭、日期点击关闭、当前年前后 50 年共 101 年滚动候选、禁止原生 select 回归、编辑桥返回语义文档化和合同化均已处理。
  confidence_assessment: HIGH — [`node scripts/check-theater-contract.cjs`](scripts/check-theater-contract.cjs:1)、[`npm run lint --silent`](package.json:8)、[`npm run build --silent`](package.json:7) 已连续通过；无已知未验证的关键假设。

# 产物实质性检查
substance_check:
  - 产物中是否存在"形式完整但实质空洞"的内容？
    → NO。逐一检查：[`calendar.js`](modules/phone-theater/scenes/calendar.js:277) 的 `buildYearOptions()` 实际生成 101 个年份候选，`renderYearPicker()` 实际渲染内部 picker，`bindInteractions()` 实际处理 `toggle-year-picker` 和 `select-year`；[`calendar.css`](styles/phone-theater/calendar.css:126) 的 `.phone-theater-calendar-year-panel` 实际提供 `max-height`、`overflow-y: auto` 和 `overscroll-behavior: contain`；[`check-theater-contract.cjs`](scripts/check-theater-contract.cjs:346) 实际检测原生 `<select` 禁用、picker 结构、滚动样式和文档语义；两个文档都记录了编辑桥返回语义。删除任意一项都会降低系统行为或回归保障，不是空洞文字。
  - 产物是否能被其目标对象的变化所"击穿"？
    → NO。若重新在 [`calendar.js`](modules/phone-theater/scenes/calendar.js:304) 引入 `<select`，合同脚本的“calendar scene 禁止原生年份 select 弹层”会失败；若移除 `.phone-theater-calendar-year-panel` 或 `overflow-y: auto`，CSS 合同会失败；若删掉 [`docs/architecture-guide.md`](docs/architecture-guide.md:130) 的小剧场编辑桥说明，`architectureGuide` 合同会失败；若删掉 [`docs/reference/theater-scene-extension-spec.md`](docs/reference/theater-scene-extension-spec.md:101) 的返回来源美化页说明，`specDoc` 合同会失败。
  - 实质性比率: 8/8 = 100%。

# 覆盖完整性检查
completeness_check:
  - 是否存在被跳过的模块/函数/路径？
    → NO。需求涉及的小日历 scene、样式、编辑桥架构文档、扩展规范、合同脚本和 dist 构建产物均已覆盖。没有把“不好测的浏览器弹层”继续交给 CSS 硬扛。
  - 产物覆盖的范围是否与 execution_plan 中 affected_scope 完全一致？
    → YES。affected_scope 的 [`docs/architecture-guide.md`](docs/architecture-guide.md:120)、[`docs/reference/theater-scene-extension-spec.md`](docs/reference/theater-scene-extension-spec.md:79)、[`modules/phone-theater/scenes/calendar.js`](modules/phone-theater/scenes/calendar.js:277)、[`styles/phone-theater/calendar.css`](styles/phone-theater/calendar.css:89)、[`scripts/check-theater-contract.cjs`](scripts/check-theater-contract.cjs:37)、[`dist/yuzi-phone.bundle.js`](dist/yuzi-phone.bundle.js)、[`dist/yuzi-phone.bundle.css`](dist/yuzi-phone.bundle.css)、[`.analysis-cache.md`](.analysis-cache.md:1) 均已处理。
  - 核心业务逻辑是否都有直接验证（不依赖间接覆盖）？
    → YES。核心业务逻辑包括：禁止原生年份 select，直接由 [`check-theater-contract.cjs`](scripts/check-theater-contract.cjs:346) 检查；自定义 picker 交互关键 token，直接由 [`check-theater-contract.cjs`](scripts/check-theater-contract.cjs:347) 检查；picker 滚动样式，直接由 [`check-theater-contract.cjs`](scripts/check-theater-contract.cjs:348) 检查；编辑桥返回语义，直接由 [`check-theater-contract.cjs`](scripts/check-theater-contract.cjs:386) 和 [`check-theater-contract.cjs`](scripts/check-theater-contract.cjs:387) 检查；打包产物由 [`npm run build --silent`](package.json:7) 验证生成。
  - affected_scope 覆盖率: 8/8 = 100%。

# 价值密度检查
value_density_check:
  - 产物中高价值内容与低价值内容的比例是多少？
    → 高价值:低价值 = 8:0，高价值占比 100%。高价值内容是替换原生弹层交互模型、加入可滚动 picker、文档化返回合同、合同脚本真实读取架构文档、更新 dist；没有用 trivial 文案或无效检查凑数量。
  - 是否存在"用数量掩盖质量"的模式——大量 trivial 产物掩盖了核心逻辑缺少验证的事实？
    → NO。产物数量不多，但每项都直接对应用户反馈的缺陷链路：黑色下拉、年份范围有限、不能滚动、编辑返回语义未写入架构。合同脚本也不是摆设，已通过验证命令确认。

# 需求对齐检查
alignment_check:
  - 产物满足的是用户的字面需求还是本质需求？
    → 满足本质需求。字面需求是“下拉框黑、不能随意年份、编辑返回逻辑写进架构”；本质需求是小日历在 SillyTavern 深色主题下仍可维护、可操作、可回归验证。实现没有继续修补原生 select，而是改成内部 picker；年份范围从 9 项扩展为当前年前后 50 年共 101 项并支持滚动；编辑桥返回来源美化页被写入 [`docs/architecture-guide.md`](docs/architecture-guide.md:130) 和 [`docs/reference/theater-scene-extension-spec.md`](docs/reference/theater-scene-extension-spec.md:79)。
  - "如果这是别人交给我的，我会接受吗？"
    → YES。因为它不是“能跑就行”的补丁，而是替换了错误交互模型、补齐了文档合同、加入了回归检查并通过了合同、lint、build。唯一仍可进一步提升的是让年份 picker 自动滚动到当前选中年份附近；当前打开面板时已选年份位于 101 项中间区域，但浏览器默认滚动位置可能从顶部开始。这个不是阻断交付的问题，因为年份范围和滚动能力已具备，但下一步若追求更顺滑体验，可以在打开时对 `.is-selected` 调用 `scrollIntoView({ block: 'center' })`。本轮没有加入，是因为当前需求核心是摆脱黑色原生弹层和有限年份，不应引入额外滚动副作用。
</output_quality_review> 
---
<decision_point>
issue: 复查 [`scripts/check-theater-contract.cjs`](scripts/check-theater-contract.cjs:6) 时发现我把架构文档检查错误地挂在 [`specDoc`](scripts/check-theater-contract.cjs:37) 上。当前 [`FILES`](scripts/check-theater-contract.cjs:6) 只有 [`specDoc`](scripts/check-theater-contract.cjs:37)，没有 [`architectureGuide`](docs/architecture-guide.md:120) 对应 key；如果保持这样，所谓“架构文档说明”检查实际仍在读 [`docs/reference/theater-scene-extension-spec.md`](docs/reference/theater-scene-extension-spec.md:79)。这不是检查，是自欺欺人的假检查。
impact: YES。影响 [`scripts/check-theater-contract.cjs`](scripts/check-theater-contract.cjs:386) 到 [`scripts/check-theater-contract.cjs`](scripts/check-theater-contract.cjs:387) 的合同有效性。若不修，架构文档被删改也不会被合同脚本发现，回归检查没有实质价值。
context_update: 原 [`analysis`](.analysis-cache.md:1) 中 `affected_scope` 已包含 [`docs/architecture-guide.md`](docs/architecture-guide.md:120) 与 [`scripts/check-theater-contract.cjs`](scripts/check-theater-contract.cjs:23)，但执行计划低估了合同脚本需要新增文件 key 的事实。
options:
  - option_a:
      description: 在 [`FILES`](scripts/check-theater-contract.cjs:6) 中新增 `architectureGuide: 'docs/architecture-guide.md'`，然后把架构文档合同检查从 [`specDoc`](scripts/check-theater-contract.cjs:37) 改为读取 [`architectureGuide`](docs/architecture-guide.md:120)。
      approach_evaluation: 可维护性 5/5，文件 key 与检查对象一致；健壮性 5/5，能真实检测架构文档；可扩展性 5/5，后续架构文档合同检查可继续复用该 key。
      edge_cases: 若 [`docs/architecture-guide.md`](docs/architecture-guide.md:120) 不存在，文件存在性检查会失败；若只改规范文档不改架构文档，架构合同检查也会失败。
      affected_scope_delta: 仍只修改 [`scripts/check-theater-contract.cjs`](scripts/check-theater-contract.cjs:6)，不新增其他文件。
  - option_b:
      description: 不新增 key，直接在合同脚本里用 [`read()`](scripts/check-theater-contract.cjs:132) 手动读取 [`docs/architecture-guide.md`](docs/architecture-guide.md:120) 到局部变量。
      approach_evaluation: 可维护性 3/5，会绕开统一 [`FILES`](scripts/check-theater-contract.cjs:6) 机制；健壮性 4/5，可以读到正确文档，但缺少统一存在性检查；可扩展性 2/5，后续检查会继续散落局部读取。
      edge_cases: 文件缺失时错误路径不如统一存在性检查清晰。
      affected_scope_delta: 仍只修改 [`scripts/check-theater-contract.cjs`](scripts/check-theater-contract.cjs:132)。
  - option_c:
      description: 删除架构文档合同检查，只保留扩展规范文档检查。
      approach_evaluation: 可维护性 2/5，减少检查但让架构文档无门禁；健壮性 1/5，无法防止编辑桥返回语义从架构文档中丢失；可扩展性 1/5，继续制造隐性规则。
      edge_cases: 后续有人只读 [`docs/architecture-guide.md`](docs/architecture-guide.md:120) 会看不到编辑桥合同，错误实现仍可能复发。
      affected_scope_delta: 缩小检查范围，属于质量下降。
recommendation: 选择 option_a。三维评分显示 option_a 在可维护性、健壮性、可扩展性均为 5/5；option_b 绕开统一文件表，option_c 直接降级。
execution_plan_update: 在原 step_6 中增加子步骤：给 [`FILES`](scripts/check-theater-contract.cjs:6) 增加 `architectureGuide` key，并把架构文档相关 [`pushCheck()`](scripts/check-theater-contract.cjs:144) 改为使用 `architectureGuide`，而不是错误复用 `specDoc`。
deviation_audit:
  original_plan_excerpt: `- step_6: 修改 [`scripts/check-theater-contract.cjs`](scripts/check-theater-contract.cjs:346)，将“原生下拉隔离”检查替换为“禁止原生年份下拉 + 自定义年份 picker class 齐全 + picker 滚动样式齐全”；同时补充架构文档和扩展规范的编辑桥返回语义检查。`
  current_proposal: 在同一个 step_6 中补充正确的 `architectureGuide` 文件 key，并让架构检查读取 [`docs/architecture-guide.md`](docs/architecture-guide.md:120)。
  diff_summary: 新增一个文件 key；修正两个架构文档检查的读取对象；没有删除原计划内容；没有缩小范围。
  deviation_motive_check:
    - 措辞替换规则逐类检查：未命中降级措辞。
    - 偏离后的方案在三维评分上是否低于原方案？→ NO。当前方案提高合同脚本真实性。
    - 偏离是否导致 affected_scope 缩小？→ NO。仍覆盖原 affected_scope。
  self_dissection: 未触发。
degradation_check:
  - 推荐方案是否是三维评估综合最优的？ → YES。option_a 为 5/5、5/5、5/5。
  - 推荐方案是否遗漏了新发现的边界条件？ → NO。已覆盖文件缺失、读错文档、检查失效。
  - 是否因为想尽快完成而选择了改动量小的方案？ → NO。没有选择删除检查的 option_c。
  - 修改后的execution_plan是否仍覆盖所有affected_scope？ → YES。仍覆盖 [`scripts/check-theater-contract.cjs`](scripts/check-theater-contract.cjs:6) 与 [`docs/architecture-guide.md`](docs/architecture-guide.md:120)。
  - 是否有发现了但被判断为"无关紧要"而跳过的问题？ → NO。读错文档是合同脚本实质缺陷，已纳入修正。
  - options 是否包含至少三个方案？ → YES。
  - 是否因为工具报错而准备换用 shell 命令修改源代码？ → NO。继续使用编辑工具。
  - deviation_audit 是否触发了 self_dissection？ → NO。
</decision_point> 