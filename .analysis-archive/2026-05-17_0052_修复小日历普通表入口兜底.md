<analysis>
context: 本轮修复不是继续做新功能，而是修复真实截图暴露出的入口分流缺陷。截图显示页面标题为“小日历表”，包含通用表列表的搜索框、倒序按钮、7条记录和新增/锁定/删除底栏，这证明当前点击进入的是普通 `app:<sheetKey>` 表格查看器，而不是小剧场 `theater:calendar` 美化页。已读取 `modules/phone-home/view-model.js`：首页会在第 44 行计算 `groupedTheaterSheetKeys = getGroupedTheaterSheetKeys(rawData)`，第 47 行把 `getAvailableTheaterScenes(rawData)` 生成的 scene 虚拟 App 加入 apps，第 73 行过滤已成组子表；因此正常情况下“小日历表”不应作为普通表 App 出现。已读取 `modules/phone-theater/data.js`：`getAvailableTheaterScenes(rawData)` 依赖 `resolveTheaterSceneTables` 判定主表存在，`getGroupedTheaterSheetKeys(rawData)` 只在 scene available 时把 childSheetKeys 加入隐藏集合，`resolveTheaterSceneBySheetKey(rawData, sheetKey)` 已经存在并可从 sheetKey 反查 scene。已读取 `modules/phone-theater/scenes/index.js` 与 `modules/phone-theater/scenes/calendar.js`：calendar scene 已注册，`tables.days` 绑定 `小日历表`，`appKey` 为 `__theater_calendar`，route 为默认 `theater:calendar`。已读取 `modules/phone-core/route-renderer.js`：普通 `app:` 分支在第 38-47 行直接调用 `renderTableViewer(page, sheetKey)`；`table-generic:` 分支在第 49-58 行调用 `renderTableViewer(page, sheetKey, { forceGenericList: true })`；`theater:` 分支在第 60-69 行调用 `renderTheaterScene(page, sceneId, { renderToken })`。当前缺陷就是普通 `app:` 分支没有二次兜底：一旦首页层没有隐藏子表或用户从通知、历史 route、外部 slash/旧入口打开 `app:<小日历sheetKey>`，系统会直接进入通用表。已读取 `modules/table-viewer/render.js`：`forceGenericList` 已能保证编辑桥绕过 special，不能被 app 兜底拦截。已读取 `scripts/check-theater-contract.cjs`：当前只检查首页生成组合 app、隐藏子表、route 支持 theater 和 table-generic，但没有检查普通 `app:` 路由是否会把小剧场子表转给 theater；因此合同没能击穿这次真实问题。dist 中已确认包含 calendar scene 与 CSS，所以问题不是构建丢失，而是入口路径没进 scene。
needs: 用户要求按方案修复“没有美化上”的问题。本质需求是：无论小日历表从首页普通表入口、历史 route、通知或其他 `app:<sheetKey>` 入口进入，只要它属于小剧场 scene，就应该进入美化页；只有显式编辑桥 `table-generic:<sheetKey>` 才能进入原始通用表列表。这个修复还必须保持后续广场、论坛、直播等 scene 可复用，不能只写 `小日历表` 特判。
key_challenges: 1) 不能破坏编辑桥：`table-generic:<sheetKey>` 必须继续强制通用列表，否则右上编辑会死循环回美化页。2) 普通 `app:` 兜底需要读取当前表数据来判断 sheetKey 属于哪个 scene，现有可用 API 是 `getTableData()` 与 `resolveTheaterSceneBySheetKey(rawData, sheetKey)`；这两个应在 route-renderer 动态导入，避免顶层循环依赖。3) route-renderer 的 app 分支目前同步返回 renderer 对象；新增异步动态 import 仍在 `async loadRouteRenderer` 内，可控。4) 如果 scene 解析失败或表不是 theater 子表，必须保留原普通表渲染，不允许让普通表入口空白。5) 必须同步 contract 脚本，否则这类入口兜底缺失以后还会回归。6) 这次缺陷说明之前验收只做静态构建不够，修复后至少要用 contract 明确检查 `resolveTheaterSceneBySheetKey` 在 app 分支参与判断。
confidence: HIGH
  - HIGH: 入口路径、现象、已存在 helper、编辑桥边界和契约缺口均已定位；所需改动集中在 `modules/phone-core/route-renderer.js` 和 `scripts/check-theater-contract.cjs`，验证链路明确。唯一无法在当前工具内完全完成的是真实宿主浏览器点击，但代码路径本身已足够清楚。
approach: 三维评估综合最优的方案是在普通 `app:` route 分支中增加小剧场子表兜底，同时保留 `table-generic:` 作为唯一强制通用表入口，并把该规则写入 theater contract。
  - 可维护性: 5/5 — 使用现有 `resolveTheaterSceneBySheetKey`，不新增 calendar 硬编码；规则放在 route-renderer 的入口层，符合“入口分流问题在入口层处理”的职责边界。
  - 健壮性: 5/5 — 覆盖首页漏隐藏、通知、历史 route、外部直接打开 `app:<sheetKey>` 等路径；解析不到 scene 时降级为原通用表；`table-generic:` 不受影响。
  - 可扩展性: 5/5 — 后续广场、论坛、直播或新增 scene 都复用 `resolveTheaterSceneBySheetKey`，无需为每个 scene 写分支。
edge_cases: 1) `app:` 的 sheetKey 为空时不能误进 theater，应继续让 `renderTableViewer` 处理并显示原错误路径。2) rawData 不存在或 table index 找不到 sheetKey 时，app 分支必须走原通用表逻辑。3) sheetKey 属于小剧场子表且 scene available 时，app 分支应该调用 `renderTheaterScene(page, scene.id, { renderToken })`，不是 `navigateTo`，避免在渲染阶段改写 routeHistory 造成回退栈污染。4) `table-generic:<sheetKey>` 必须跳过兜底，继续进入 `renderTableViewer(page, sheetKey, { forceGenericList: true })`。5) scene 主表和附表都应能被 app 兜底转到对应 scene；这对广场三表、论坛三表、直播双表同样成立。6) 如果 scene 可用性因为主表缺失而 false，附表单独存在时不应进 theater 空壳。7) routeRenderer 动态 import 失败必须沿用现有 `resolveRouteRenderer` catch 进入统一 logger，不要裸抛到 UI。8) contract 检查必须直接验证 app 分支含 `resolveTheaterSceneBySheetKey`、`getTableData`、`renderTheaterScene(page, theaterScene.id, { renderToken })`，否则又是看起来有脚本、实际漏关键行为。
affected_scope: `modules/phone-core/route-renderer.js`; `scripts/check-theater-contract.cjs`; `dist/yuzi-phone.bundle.js`; `dist/yuzi-phone.bundle.js.map`; `.analysis-cache.md`; `.analysis-archive/*`.
execution_plan:
  - step_1: 修改 `modules/phone-core/route-renderer.js` 的 `app:` 分支：在得到 `sheetKey` 后动态导入 `getTableData` 与 `resolveTheaterSceneBySheetKey`，用当前 rawData 解析 theater scene；如果存在 scene，则动态导入 `renderTheaterScene` 并返回 `routeType: 'theater-app-redirect'` 的 renderer，渲染时调用 `renderTheaterScene(page, theaterScene.id, { renderToken })`；否则保留原 `renderTableViewer(page, sheetKey)`。
  - step_2: 确认 `table-generic:` 分支位置和逻辑不变，避免编辑桥被 app 兜底影响。
  - step_3: 修改 `scripts/check-theater-contract.cjs`，增加 contract 检查：route-renderer 的普通 app 分支必须导入/调用 `getTableData`、`resolveTheaterSceneBySheetKey`，必须存在 `routeType: 'theater-app-redirect'` 或等价标识，必须调用 `renderTheaterScene(page, theaterScene.id, { renderToken })`，并继续检查 `table-generic` 的 `forceGenericList: true`。
  - step_4: 运行 `node scripts/check-theater-contract.cjs` 验证合同能覆盖新规则。
  - step_5: 运行 `npm run lint --silent`，确认新增动态 import 和变量命名不触发 lint。
  - step_6: 运行 `npm run build --silent`，生成更新后的 dist 产物。
  - step_7: 检查 dist 中是否包含 `theater-app-redirect` 或 `resolveTheaterSceneBySheetKey` 相关打包结果，确认构建产物包含兜底逻辑。
  - step_8: 读取 `.analysis-cache.md`，输出 `output_quality_review`，追加写入并归档到 `.analysis-archive/`。
degradation_check:
  - 方案是否是三维评估（可维护性、健壮性、可扩展性）综合最优的？ → YES。方案三维均为 5/5；相比只修首页隐藏，它覆盖所有 `app:<sheetKey>` 入口；相比在 calendar 内特判，它对所有 scene 复用。
  - 是否遗漏了已知边界条件？ → NO。已覆盖空 sheetKey、rawData 缺失、scene 不可用、编辑桥不拦截、多表 scene、routeHistory 污染、动态 import 失败、contract 覆盖。
  - 是否因改动量大而想缩减方案？ → NO。改动量不大，但仍处理 contract 和 build 产物，不只改主文件。
  - 是否打算跳过某些文件？ → NO。route-renderer、contract、dist、analysis 归档都在 affected_scope 和 execution_plan 内。
  - execution_plan是否覆盖affected_scope所有文件？ → YES。每个 affected_scope 文件都有对应步骤：源码 step_1/2，contract step_3/4，dist step_6/7，analysis step_8。
  - context是否充分？是否有未读但可能相关的文件？ → NO。已读取 route、home view-model、theater data、table viewer、contract，以及前一轮确认 dist 含 calendar；足够定位并修复入口兜底。
  - 是否有发现了但被我判断为"无关紧要"而跳过的问题？ → NO。真实浏览器点击未验证不是无关紧要，已作为后续验收边界保留；本轮先修代码路径。
  - execution_plan中是否有步骤计划使用 shell 命令（sed/awk/perl）修改源代码？ → NO。源码修改使用编辑工具；shell 只用于 contract、lint、build、只读验证。
</analysis>
---
<output_quality_review>
task_summary: 本次修复解决“小日历表点击后进入通用列表而非美化页”的入口分流问题：普通 `app:<sheetKey>` 路由现在会先判断目标表是否属于小剧场 scene，属于则渲染对应 `theater:<sceneId>` 美化页；显式 `table-generic:<sheetKey>` 继续作为编辑桥进入原始通用表列表。同步更新 theater contract，确保以后这类兜底缺失会被脚本击穿。
deliverables: 交付文件包括 `modules/phone-core/route-renderer.js`、`scripts/check-theater-contract.cjs`、`dist/yuzi-phone.bundle.js`、`dist/yuzi-phone.bundle.js.map`、`.analysis-cache.md` 与归档记录。

# 量化指标总览
metrics:
  total_files_modified: 5 — 2 个源码/契约文件、2 个构建产物、1 个分析归档文件。
  execution_plan_coverage: 8/8 = 100% — app 路由兜底、table-generic 保留、contract 更新、contract/lint/build 验证、dist 检查和分析归档均已完成。
  edge_cases_handled: 8/8 = 100% — 空 sheetKey 降级、rawData 缺失降级、scene 子表进 theater、table-generic 不拦截、多表 scene 复用、scene 不可用降级、动态 import 错误走现有 catch、contract 覆盖全部处理。
  confidence_assessment: MEDIUM — 代码路径、contract、lint、build 均通过；真实 SillyTavern 宿主点击仍需要用户刷新扩展后再验证。
    - MEDIUM: 当前工具无法直接操作用户截图中的宿主页面；但 `node scripts/check-theater-contract.cjs` 已新增并通过普通 app 子表兜底检查，`npm run lint --silent` 与 `npm run build --silent` 已通过。

# 产物实质性检查
substance_check:
  - 产物中是否存在"形式完整但实质空洞"的内容？
    → NO。`route-renderer.js` 的 `app:` 分支真实调用 `getTableData()` 与 `resolveTheaterSceneBySheetKey()`，命中 scene 时渲染 `renderTheaterScene(page, theaterScene.id, { renderToken })`；`check-theater-contract.cjs` 真实检查这些关键字符串，删除兜底逻辑会导致合同失败；dist 由 build 生成并包含更新后的运行时代码。
  - 产物是否能被其目标对象（被测代码/被重构模块/被修复的bug）的变化所"击穿"？
    → YES。若删除 `resolveTheaterSceneBySheetKey(getTableData(), sheetKey)`、删除 `routeType: 'theater-app-redirect'`、改坏 `renderTheaterScene(page, theaterScene.id, { renderToken })` 或破坏 `table-generic` 的 `forceGenericList: true`，`node scripts/check-theater-contract.cjs` 会失败。若动态 import 或语法有问题，`npm run lint --silent` 或 `npm run build --silent` 会失败。
  - 实质性比率: 5/5 = 100% — 每个产物都对应运行时行为、合同保护、构建输出或决策追溯。

# 覆盖完整性检查
completeness_check:
  - 是否存在被跳过的模块/函数/路径？
    → NO。修复覆盖普通 app 入口、显式 table-generic 编辑入口、theater render 入口和 contract。没有只修小日历特判，也没有跳过广场/论坛/直播这类未来复用路径。
  - 产物覆盖的范围是否与 execution_plan 中 affected_scope 完全一致？
    → YES。`modules/phone-core/route-renderer.js` 完成 app 兜底；`scripts/check-theater-contract.cjs` 完成合同覆盖；`dist/yuzi-phone.bundle.js` 与 map 由 build 更新；`.analysis-cache.md` 正在归档。
  - 核心业务逻辑是否都有直接验证（不依赖间接覆盖）？
    → YES。普通 app 子表兜底由 theater contract 直接验证；lint 直接验证语法和静态问题；build 直接验证打包产物生成；dist 搜索确认构建包含相关逻辑。真实 UI 点击验证仍需宿主刷新后执行，已在 confidence 中明确。
  - affected_scope 覆盖率: 6/6 = 100% — affected_scope 中的文件/路径均已处理。

# 价值密度检查
value_density_check:
  - 产物中高价值内容（验证核心逻辑/处理复杂场景）与低价值内容（验证trivial行为）的比例是多少？
    → 高价值:低价值 = 4:1，高价值占比 80%。高价值内容是 app 路由兜底、table-generic 保护、contract 覆盖和 build 产物；低价值但必要的是分析归档。
  - 是否存在"用数量掩盖质量"的模式——大量 trivial 产物掩盖了核心逻辑缺少验证的事实？
    → NO。文件数量很少，核心逻辑集中且被 contract 直接检查。没有靠大批无关修改制造“修了很多”的假象。

# 需求对齐检查
alignment_check:
  - 产物满足的是用户的字面需求还是本质需求？
    → 满足本质需求。用户要的是“小日历表不要再显示普通列表，而是进入美化页”。本修复不是调 CSS，而是修正入口分流：只要表属于 theater scene，普通 `app:` 入口也会进入 scene；只有编辑桥 `table-generic:` 才进入通用表。这正好对应截图暴露的真实问题。
  - "如果这是别人交给我的，我会接受吗？"
    → YES，但我会要求刷新扩展后再用真实宿主点一次“小日历表”。代码修复和静态验证达标；没做真实点击就宣称 100% 闭环，那是自欺欺人。这里我不粉饰。

→ 验收未发现需要就地修正的代码问题。真实宿主点击验证仍需在用户环境刷新扩展后执行。
</output_quality_review>
